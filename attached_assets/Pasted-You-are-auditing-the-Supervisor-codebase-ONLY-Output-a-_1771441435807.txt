You are auditing the Supervisor codebase ONLY. Output a precise “state of truth” report so we can implement this plan in one day.

PLAN TARGETS:

Canonical delivery_summary object produced server-side and used everywhere.
Rules:
PASS: verified_exact >= user_requested AND Tower != STOP
PARTIAL: verified_exact > 0 AND Tower != PASS
STOP: Tower verdict STOP or CHANGE_PLAN
Raw counts must NEVER imply success.

Belief Store (v0): 2-3 beliefs per run derived from CVL failures + Tower stop reasons.

Goal Ledger (v0): every run belongs to a goal; STOP always has a reason; goals persist across runs.

Feedback Signal Logging: accept_result, retry_goal, abandon_goal, export_data linked to goal_id + run_id.

Demo Orchestrator (manual): 3 known demo cases to drive narrative.

DO THIS:

Identify where delivery_summary is currently created:

what inputs it uses (leads length, target_count, etc)

where it ignores CVL/tower

list exact files/functions and current logic.

Identify how CVL results are stored and referenced:

where verification_summary is computed

where it is saved (db table? artefact? run record?)

exact fields: verified_exact, hard_unverifiable, hard_unknown etc.

Identify how Tower verdict is stored and retrieved:

where judgement is requested/emitted

where verdict is persisted (artefact? run row? activity?)

how “STOP / CHANGE_PLAN / ACCEPT / RETRY” are represented in data.

Identify current schema/tables used in Supabase for runs + artefacts + activity:

list tables and key columns used for run display

include any existing “goals” or “beliefs” tables (if they exist).

Produce a “Supervisor delta plan”:
A) Add/extend API response to include canonical delivery_summary based on Tower + CVL.
B) Add belief_store table + writeBeliefs(run) function (max 3 beliefs).
C) Add goal_ledger table + ensure every run has goal_id and status updated from delivery_summary.
D) Add feedback_events table + endpoints for accept/retry/abandon/export.
E) Add demo orchestrator endpoints for 3 preset scenarios.

OUTPUT FORMAT:

Section A: Where delivery_summary currently comes from

Section B: CVL state (where computed, where stored, fields)

Section C: Tower verdict state (where computed, where stored, fields)

Section D: Existing DB tables relevant

Section E: Minimal schema additions (SQL)

Section F: Minimal code changes (by file)

Section G: Risks/unknowns (max 5)

Do not implement yet. Only audit + deltas.