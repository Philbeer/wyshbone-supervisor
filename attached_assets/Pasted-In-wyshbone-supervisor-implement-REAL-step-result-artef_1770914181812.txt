In wyshbone-supervisor, implement REAL step_result artefacts for the UUID UI-triggered runs (run_id like d291316d-...). This is strictly additive.

Absolute rules

Do NOT remove, gate, or change anything that already works (manual judgement must still work).

Do NOT suppress end-of-run judgement or existing tower_judgement behaviour.

Add step_result artefacts ON TOP.

Where to implement
File: server/supervisor.ts
Functions to instrument (these are the UUID chat flows):

executeTowerLoopChat()

generateLeadsForChat()

executeDeepResearchForChat()

What to add

Create a helper in this file (or reuse existing helpers if already available in this file):

isStepArtefactsEnabled(): boolean (reads ENABLE_STEP_ARTEFACTS, default true)

redactSecrets(input): returns redacted object (no tokens/headers/cookies/api keys)

safeOutputsRaw(output): returns { outputs_raw?, outputs_raw_omitted? } with 50k char cap

For EACH function above:

Capture stepStartedAt = Date.now() before the primary tool call begins

At the boundary where the step is known success/fail, call createArtefact(...) with:
type: "step_result"
title: "Step result: <short human step name>"
summary: 1-2 lines, include success/fail and short outcome
payload_json must include:
run_id: <chatRunId>
client_request_id: <clientRequestId>
goal: (if available)
plan_version: 1
step_id: a stable id string per function (e.g. "chat_search_places", "chat_generate_leads", "chat_deep_research")
step_title: human title
step_index: 0
step_status: "success" | "fail"
inputs_summary: redacted inputs/tool args
outputs_summary: small object/string (count, notes)
outputs_raw: optional only if <= 50k chars, else outputs_raw_omitted true
timings: started_at, finished_at, duration_ms

Artefact write must NOT crash the run:
try/catch around createArtefact; log warning; continue.

Insertion points

executeTowerLoopChat():

Success/stub: after the tool call completes and you have the results (around where step_completed is logged), before leads_list artefact is created.

Exception: in the catch block, write step_result fail.

generateLeadsForChat():

Missing business type early return: write step_result fail before returning.

Zero results: write step_result fail after search returns empty.

Success: write step_result success after search results are obtained (or after leads persisted), but before final artefacts.

Outer catch: write step_result fail.

executeDeepResearchForChat():

Write step_result success after research returns.

Write step_result fail in catch.

Deliverables

Rewrite the FULL CONTENT of server/supervisor.ts in your output if you edit it (Phil needs full-file copy paste).

Keep changes minimal and additive.

Confirm with a short verification checklist:

Run a UI-triggered chat flow that produces UUID run_id

Supabase query:
select created_at, type, title from artefacts where run_id = '<UUID>' order by created_at asc;
should now include type='step_result'

Manual judgement still works unchanged