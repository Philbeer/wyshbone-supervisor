You are implementing Part 3 MVP Learning Plan execution correctness in the Wyshbone Supervisor repo.

Goals (must):
1) Learned max_replans from applied policy bundle must be authoritative in plan execution. Remove or bypass any hard-coded MAX_PLAN_VERSIONS limit that conflicts. If you keep a safety hard cap, make it explicit:
   - learned_max_replans
   - hard_cap_max_replans
   - effective_max_replans
   Persist all three in decision_log and outcome_log artefacts.
2) Tower unreachable behaviour must be honest and deterministic:
   - If Tower cannot be reached for a required judgement, emit an artefact or outcome flag that Tower was unavailable.
   - Do not silently "continue as if judged". Either STOP (preferred for honesty) or CONTINUE but mark the run as ungoverned and force the UI to show Tower not available. Pick one policy and apply consistently.
   - Whatever you choose, ensure stop_reason or governance_status is present in delivery_summary or outcome_log.
3) Persist total retry count per run (sum of per-step retries) into outcome_log payload.
4) Add idempotency_key support for Tower judgement persistence:
   - idempotency_key = runId + artefactId + stepIndex + planVersion (or equivalent)
   - ensure DB insert for tower_judgements is deduped by idempotency_key (add unique constraint if schema supports, or dedupe in storage).
5) Apply per-run overrides passed from UI message metadata:
   - mode preset: faster|balanced|stricter
   - override_max_replans: 0-3
   - ignore_learned_policy: boolean
   These overrides must affect applied policy bundle at run start and be recorded in policy_application_snapshot, decision_log, and outcome_log.
6) Add or ensure a simple "Reset scope key to defaults" backend action exists (API route can be added in supervisor server routes if this repo owns it). This should write a new policy_versions row equal to global default for that scope key and mark it as reset_reason="user_reset".

Hard rules:
- Do not change the stable decision tree.
- Do not add new tools.
- Do not claim Tower judged when it did not.
- Keep changes minimal and auditable. Prefer writing explicit artefacts over implicit logs.
- No long dashes.

Deliverables:
- List all files changed.
- Update any relevant shared schema if needed and include migration steps.
- Provide a short acceptance test checklist.

Notes:
- The UI already parses policy_application_snapshot and outcome_log. Add fields there rather than inventing brand new types unless needed.