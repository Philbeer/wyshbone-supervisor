You are implementing the “WYSHBONE 1 DAY DEMO USABLE PLAN (CANONICAL)” in the Supervisor repo. This repo MUST be the single source of truth for delivery status and persistence.

CRITICAL DB RULE (read carefully):

This project uses Supabase Postgres as the ONLY database.

Replit often auto-writes to an internal local DB (SQLite or file DB). That is forbidden.

You must identify any code paths that use a local DB adapter or default connection and remove/disable them so that all reads/writes go through Supabase Postgres.

If there is any fallback to sqlite, file db, or in-memory store, delete it or make it throw loudly in non-test mode.

Add a startup log line that prints the resolved database host (but never prints secrets) so we can confirm it is Supabase.

GOAL:
Make Wyshbone honest, persistent, and demo-credible.
Not polished. Not autonomous. But defensible.

PHASE 1: TRUTH CLOSURE
Implement a single canonical delivery_summary object derived from:

Tower verdict (canonical)

CVL verification_summary (verified_exact_count)

requested_count_user
Raw lead counts must NEVER imply success.

Canonical delivery status:

PASS: verified_exact_count >= requested_count_user AND tower_verdict != STOP

PARTIAL: verified_exact_count > 0 AND tower_verdict != ACCEPT

STOP: tower_verdict in (STOP, CHANGE_PLAN)

Notes:

We are using Tower’s canonical verdict enum: ACCEPT | CHANGE_PLAN | STOP

If Tower verdict is missing, treat as STOP with stop_reason code "MISSING_TOWER_VERDICT" (this prevents lying).

delivery_summary payload must include:
{
status: "PASS" | "PARTIAL" | "STOP",
requested_count_user: number,
verified_exact_count: number,
delivered_exact: Lead[],
delivered_closest: Lead[],
shortfall: number,
stop_reason?: { code: string, message: string, evidence?: object },
tower_verdict: "ACCEPT" | "CHANGE_PLAN" | "STOP",
cvl_summary?: { hard_unverifiable: string[], hard_unknown: number, verified_exact_count: number }
}

PHASE 2: PERSISTENCE
Add three Supabase tables and wire them properly:

belief_store (v0)

belief_id uuid pk default gen_random_uuid()

run_id uuid not null

goal_id uuid null

claim text not null

confidence numeric(3,2) not null (0 to 1)

evidence_run_ids uuid[] not null default '{}'

evidence jsonb not null default '{}'

last_updated timestamptz not null default now()

Rules:

Max 3 beliefs written per run.

Beliefs derived ONLY from:

CVL failures and verification_summary

Tower stop_reason
Examples:
"Only 2 pubs in Arundel match NAME_CONTAINS=swan"
"Radius expansion beyond 10km yielded no new matches"

goal_ledger (v0)

goal_id uuid pk default gen_random_uuid()

user_id uuid not null

goal_text text not null

success_criteria jsonb not null

status text not null check in ('ACTIVE','PARTIAL','STOPPED','COMPLETE')

linked_run_ids uuid[] not null default '{}'

stop_reason jsonb null

created_at timestamptz not null default now()

updated_at timestamptz not null default now()

Rules:

Every run must belong to a goal_id.

STOPPED always has stop_reason.

Survive multiple runs.

feedback_events (v0)

event_id uuid pk default gen_random_uuid()

user_id uuid not null

goal_id uuid not null

run_id uuid not null

event_type text not null check in ('accept_result','retry_goal','abandon_goal','export_data')

payload jsonb not null default '{}'

created_at timestamptz not null default now()

PHASE 3: DEMO ORCHESTRATOR (manual)
Expose 3 deterministic demo endpoints that create a goal and trigger a run:

demo_clean_success

demo_partial_delivery

demo_intelligent_stop
These endpoints must:

create or reuse a goal_ledger row

trigger the existing run mechanism (same as chat)

return { goal_id, run_id, client_request_id }

PHASE 4: FEEDBACK SIGNAL LOGGING
Add POST endpoints:

POST /api/feedback/accept

POST /api/feedback/retry

POST /api/feedback/abandon

POST /api/feedback/export
All require goal_id + run_id. Write into feedback_events in Supabase.

IMPLEMENTATION REQUIREMENTS:

CVL must run in every leads execution path (not just chat). Identify all call sites of delivery_summary creation and ensure they receive verification_summary and verified_exact_count.

Tower verdict must be retrieved consistently. If Tower now persists verdicts in tower_verdicts, Supervisor should read that table (Supabase) or call Tower and then persist, but the final verdict used must be persisted and consistent.

All artefacts and these new tables must be written to Supabase. No local db.

DELIVERABLES:
A) Provide SQL migration file(s) under /supabase/migrations (or /migrations) with the three tables above.
B) Update server code to compute and emit canonical delivery_summary.status and stop_reason.
C) Update routes to support demo orchestrator and feedback endpoints.
D) Add a minimal “db health check” function that asserts we are connected to Supabase Postgres (not sqlite) and fails fast otherwise.

ACCEPTANCE TESTS (must pass):

Run: "Find 4 pubs in Arundel with swan in the name"

verification_summary.verified_exact_count is 0

Tower verdict STOP or CHANGE_PLAN

delivery_summary.status MUST be STOP (never PASS)

Run: request 7, deliver 20 raw but verified_exact_count = 0

delivery_summary.status MUST be STOP or PARTIAL, never PASS

After a run completes, Supabase tables contain:

goal_ledger row with linked_run_ids contains the run_id

belief_store contains <= 3 rows for that run_id

Feedback endpoint writes a feedback_events row linked to the same goal_id + run_id

Do not handwave. Make the changes. If any files are large, still produce full file rewrites. At the end, output:

list of files changed

the SQL migrations

exact curl commands to test the new endpoints