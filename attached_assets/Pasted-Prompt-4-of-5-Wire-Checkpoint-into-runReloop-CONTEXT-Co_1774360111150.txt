Prompt 4 of 5 — Wire Checkpoint into runReloop
CONTEXT: Continuing 6.2 crash recovery. resume.ts is done. Now we wire the checkpoint into the main loop.

TASK: Modify `server/supervisor/reloop/loop-skeleton.ts` to accept and use a ResumeCheckpoint.

1. Add import at the top:
```typescript
   import type { ResumeCheckpoint } from './types';
```

2. Add `checkpoint?: ResumeCheckpoint | null;` to the `runReloop` params object (the last field, after executionPath).

3. After the existing state initialisation block (where `loopHistory`, `accumulatedEntityMap`, `executorsTriedSoFar`, `loopNumber`, `finalRawResult`, `lastGateDecision` are declared as empty/zero), add a checkpoint restoration block:
```typescript
   // ── Restore state from checkpoint if resuming ──
   const isResuming = !!params.checkpoint?.canResume;
   const resumeChainId = params.checkpoint?.chainId;

   if (isResuming && params.checkpoint) {
     const cp = params.checkpoint;
     console.log(`[RELOOP_SKELETON] RESUMING from checkpoint: lastCompletedLoop=${cp.lastCompletedLoop} resumeFrom=${cp.resumeFrom} chainId=${cp.chainId}`);

     // Use the original chain ID so loop_state rows stay grouped
     // Override the chainId declared above
     Object.defineProperty(/* can't redefine const */);
```

   Actually, since `chainId` is declared as `const`, change the declaration from:
```typescript
   const chainId = randomUUID();
```
   to:
```typescript
   const chainId = params.checkpoint?.chainId ?? randomUUID();
```

   Then in the restoration block:
```typescript
   if (isResuming && params.checkpoint) {
     const cp = params.checkpoint;
     console.log(`[RELOOP_SKELETON] RESUMING from checkpoint: lastCompletedLoop=${cp.lastCompletedLoop} resumeFrom=${cp.resumeFrom} entities=${cp.accumulatedEntities.length}`);

     // Restore accumulated entities
     for (const entity of cp.accumulatedEntities) {
       accumulatedEntityMap.set(normaliseEntityName(entity.name), entity);
     }

     // Restore loop history
     loopHistory.push(...cp.loopHistory);

     // Restore executors tried
     executorsTriedSoFar.push(...cp.executorsTriedSoFar);

     // Set loop number to resume from
     loopNumber = cp.lastCompletedLoop;

     // Restore final raw result if available
     if (cp.finalRawResult && Object.keys(cp.finalRawResult).length > 0) {
       finalRawResult = cp.finalRawResult;
     }

     // Create diagnostic artefact for the resume
     await createArtefact({
       runId,
       type: 'diagnostic',
       title: `Crash recovery: resuming from loop ${cp.lastCompletedLoop + 1} (${cp.resumeFrom})`,
       summary: `Recovered ${cp.accumulatedEntities.length} entities from ${cp.lastCompletedLoop} completed loop(s). Resume phase: ${cp.resumeFrom}.`,
       payload: {
         chain_id: chainId,
         resume_from: cp.resumeFrom,
         last_completed_loop: cp.lastCompletedLoop,
         accumulated_entities_count: cp.accumulatedEntities.length,
         executors_tried: cp.executorsTriedSoFar,
         loop_history_count: cp.loopHistory.length,
       },
       userId,
       conversationId,
     }).catch(e => console.warn(`[RELOOP_SKELETON] recovery artefact failed (non-fatal): ${e.message}`));

     logRunEvent(runId, {
       stage: 'reloop_resume',
       level: 'info',
       message: `Resuming from loop ${cp.lastCompletedLoop + 1} (${cp.resumeFrom}). ${cp.accumulatedEntities.length} entities recovered.`,
       queryText: rawUserInput,
       metadata: {
         chain_id: chainId,
         resume_from: cp.resumeFrom,
         last_completed_loop: cp.lastCompletedLoop,
         accumulated_entities: cp.accumulatedEntities.length,
       },
     });
   }
```

4. Modify the while loop to handle the `resumeFrom: 'judge'` case. At the TOP of the while loop, after `loopNumber++` and after the circuitBreaker check, add:
```typescript
   // ── Crash recovery: if resuming from 'judge', skip planner + executor for this loop ──
   const isResumeJudgeLoop = isResuming
     && params.checkpoint?.resumeFrom === 'judge'
     && loopNumber === (params.checkpoint.lastCompletedLoop + 1);
```

   Then wrap the existing planner + executor section in an if/else:
```typescript
   let plannerDecision: PlannerDecision;
   let executorOutput: ExecutorOutput;

   if (isResumeJudgeLoop && params.checkpoint?.lastPlannerDecision && params.checkpoint?.lastExecutorOutput) {
     // Recovery: skip planner + executor, use checkpoint data
     plannerDecision = params.checkpoint.lastPlannerDecision;
     executorOutput = params.checkpoint.lastExecutorOutput;
     console.log(`[RELOOP_SKELETON] Loop ${loopNumber}: RECOVERY — skipping planner+executor, using checkpoint data. executor=${plannerDecision.executorType} entities=${executorOutput.entities.length}`);
     executorsTriedSoFar.push(plannerDecision.executorType);
   } else {
     // Normal flow: plan then execute
     plannerDecision = await plannerPlan({ ... });  // keep existing planner call exactly as-is
     
     const executorFn = getExecutor(plannerDecision.executorType);
     if (!executorFn) { ... }  // keep existing null check exactly as-is
     
     // ... keep all existing executor setup and execution exactly as-is ...
     
     executorsTriedSoFar.push(plannerDecision.executorType);
   }
```

   IMPORTANT: The existing `plannerDecision` and `executorOutput` are currently declared with `const` inside the while loop. You need to change them to `let` declarations at the top of the loop iteration and then assign in both branches. Move the existing `const plannerDecision = await plannerPlan(...)` into the else branch as `plannerDecision = await plannerPlan(...)`. Same for executorOutput — the existing executor call and its try/catch should go into the else branch.

   The existing code after the executor (the `if (Object.keys(executorOutput.rawResult).length > 0)` block, the judgeEvaluate call, gateDecide call, entity accumulation, etc.) should remain OUTSIDE the if/else — it runs for both recovery and normal paths.

5. Also update the Phase 1 Supabase write (from prompt 2) to be inside the else branch only — we don't want to re-write the phase 1 row during recovery since it already exists.

   The Phase 2 update should still run in both cases (it completes the row whether it's a fresh loop or a recovery loop).

Do NOT change the function's return type or the combined delivery section. Do NOT change any other files.