You are working in the WYSHBONE SUPERVISOR repo.

Goal:
Fix the Sussex dentist verification issue by upgrading location verification from naive substring matching to a real geo-based verification layer.

Current bug:
LOCATION_EQUALS is verified by checking if lead.address contains the literal string "sussex". This causes false negatives because Google Places formatted addresses often omit county names (town + postcode only).

Requirements (production-grade):
1) Remove / deprecate any substring matching for location verification. Do not use lead.address.includes(...) for location checks.
2) Implement a dedicated geo verification path for location constraints using lead.lat/lng.
3) Interpret "Sussex" as (East Sussex OR West Sussex).
4) Add support for these location outcomes for each lead:
   - VERIFIED_GEO: lead lat/lng is inside the target region boundary
   - OUT_OF_AREA: lead lat/lng is outside boundary
   - SEARCH_BOUNDED: lead is from a search query already bounded to the region, but we did not independently geo-verify (only allowed for SOFT constraints; see Prompt 2)
   - UNKNOWN: missing lat/lng (should be rare for Places)
5) Add evidence fields to the CVL check result:
   - method: "geo_polygon" | "geo_bbox" | "search_bounded" | "unknown"
   - region_key: e.g. "GB-SUSSEX" or "GB-WEST_SUSSEX" etc.
   - lat, lng used
   - (optional) distance_to_boundary_km if you can compute cheaply (not required)

Implementation plan (choose the simplest correct route):
Option A (preferred): polygon containment
- Add boundary polygons for East Sussex and West Sussex (GeoJSON). Check point-in-polygon.
Option B (acceptable interim): bounding boxes (bbox)
- Use bbox for each county; less accurate but still far better than substring.
Do NOT use postcode parsing as the primary method unless you must; use it only as fallback if lat/lng absent.

Acceptance tests:
- For query "dentists in Sussex", leads located in Sussex towns (Crawley, Burgess Hill, Haywards Heath, etc.) should verify location even if address string does not contain "Sussex".
- Leads outside Sussex should be marked OUT_OF_AREA.
- No lead should be marked not-verified purely due to address formatting.

Deliverables:
- Updated CVL location verification logic
- Any added region data files (geojson/bbox map)
- Unit tests for point-in-region and the Sussex interpretation
- Ensure the run artefacts reflect the new statuses

Do not change other constraint types unless necessary. Keep changes focused and correct.