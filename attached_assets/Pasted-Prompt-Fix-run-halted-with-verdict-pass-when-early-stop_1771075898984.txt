Prompt: Fix “run_halted with verdict=pass” when early-stop triggers after a Tower fail

Goal: If the run ends because we already satisfied the user’s request (early stop), it must emit run_completed, not run_halted, and must not carry forward stale shouldStop or a prior Tower fail into the terminal state.

Prompt:

Fix the early-stop / terminal event logic so we never log run_halted when the final verdict is pass.

Context:

In some runs, Tower returns fail/change_plan on the leads_list (judging against internal target or constraints), but Supervisor then early-stops because accumulated leads >= requested_count_user.

The run correctly sets finalVerdict=pass / finalAction=accept, but the code still emits run_halted because a stale shouldStop (or similar flag) remains true from the Tower fail.

Requirements:

Identify the early-stop branch (the one that triggers when accumulated leads meet requested_count_user).

When early-stop triggers:

Set a dedicated terminal reason like: terminal_reason = "early_stop_satisfied_user_goal"

Force terminal status to completed, not halted:

emit AFR event run_completed (not run_halted)

create terminal artefact with type terminal (or existing type) but:

status = "completed"

verdict = "pass"

action = "accept"

include terminal_reason

Clear or override any prior stop flags so downstream code cannot treat it as halted:

e.g. set finalTowerResult.shouldStop = false (or equivalent)

or set haltReason = null

Ensure the end-of-run summary uses the terminal status:

If final verdict is pass, the run must end as completed.

Only use halted when final verdict is fail/stop/max_replans_exceeded/etc.

Add a regression guard:

If (finalVerdict === "pass") then terminalEventType MUST be run_completed (assert or conditional).

Do not change Tower logic, replanning logic, or parsing.
Only fix the incorrect terminal classification and artefact wording.

Acceptance test:

Input: “find 4 pubs in arundel using google places search with the word swan in the name”

If early stop triggers with pass, the timeline must show run_completed and terminal artefact must read completed/pass (not halted).

Deliverables:

Code change in supervisor run finalisation logic

Updated terminal artefact wording (honest and consistent)

Quick note in logs when early stop overrides a prior Tower fail: [EarlyStop] satisfied user goal; overriding prior tower fail for terminal status