You are in the Wyshbone Supervisor repo. I need a FULL factual report explaining why chat-initiated tasks don’t result in automatic Tower judgements (without clicking Request judgement), but simulate-chat-task DOES.

Deliver a report with these sections only:

A) The agent loop contract (1 paragraph):
After each step completes, create step_result artefact, call Tower judgeArtefact, persist tower_judgement artefact, emit AFR events, and react.

B) Task intake:
- What Supabase table is polled (supervisor_tasks)?
- How does a task move from pending -> processing -> completed?
- What fields are read from the task row (run_id, client_request_id, conversation_id, user_id, request_data)?
- If run_id/client_request_id missing, what happens (exact behaviour)?

C) Chat task execution path:
- Identify the exact function that executes a chat-created task.
- Confirm whether it uses run_id/client_request_id from the task row.
- Confirm whether it creates step_result artefacts on each step.
- Confirm whether it calls Tower during execution.
- Confirm whether it writes tower_judgement artefacts during execution.
- Confirm what AFR events it emits.

D) simulate-chat-task path:
- Exact endpoint/function name.
- How run_id/client_request_id are set in this path.
- Why this path successfully creates leads_list + tower_judgement automatically.

E) The specific reason judgement is missing in chat runs:
Choose ONE:
1) Tower is never called
2) Tower called but response not persisted
3) Artefacts written to wrong run_id
4) Events emitted but UI not subscribing
Back up your choice with code evidence.

F) Minimal fix plan (no refactor):
- The smallest change to guarantee: “no Supervisor run completes without at least one tower_judgement artefact AND tower_verdict event”.
- Include an end-of-run safety net if zero judgements happened mid-run.

IMPORTANT:
- No implementation, just factual report + minimal fix outline.
- Include file paths + function names.
