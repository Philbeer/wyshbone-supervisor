Repo: wyshbone-supervisor

MODE: INVESTIGATE + REPORT ONLY. DO NOT EDIT CODE.

GOAL
Last two runs were executed with the new Google Query Mode toggle ON vs OFF, but both still returned 1 result.
I need a forensic report proving:
1) Whether `google_query_mode` actually arrived at Supervisor for each run
2) Whether Supervisor used it (and which mode it actually executed)
3) The exact final Google Places request (full query string and all params)
4) Whether pagination fired (next_page_token used) and how many pages were fetched
5) If bias mode was chosen: whether we resolved lat/lng, applied location+radius+region, or silently fell back
6) The real reason both runs returned 1 result (payload evidence, not guesses)

INPUTS YOU MUST USE
- The two run IDs from today (the two “micropubs in sussex” runs where toggle ON/OFF was used).
- Pull everything from DB: decision_log, plan payload, step payload_json for SEARCH_PLACES, artefacts, and any stored tool debug fields.

WHAT TO PRODUCE (REPORT)
Create a short report with:

A) Run summary table
For each run:
- run_id
- google_query_mode in incoming request payload (from DB)
- google_query_mode in plan/tool args (what plan said)
- google_query_mode actually used by SEARCH_PLACES (what executor used)
- businessType, location, country extracted
- final Google query string constructed
- endpoint called (textsearch)
- params included: region, location(lat/lng), radius, pagetoken usage
- maxResults requested
- pages_fetched
- results_returned
- next_page_token present? (yes/no)

B) “Toggle didn’t work” diagnosis
One of these must be proven with evidence:
- UI never sent google_query_mode
- Supervisor ignored/dropped it in mission normalization
- Planner overwrote it
- Executor never read it / defaulted always
- We’re calling a different search function than expected
- Pagination not triggered / token not used
- Query construction still includes “in {location}” in both modes
- Bias resolution failed and we fell back both times
- Location bias params were built but not actually attached to request

C) Exact payload excerpts (minimal)
Paste the exact JSON snippets (not huge dumps) showing:
- incoming request payload field
- toolArgs passed into SEARCH_PLACES
- the final URL (or the map of params) used for the Google request
- the list length returned from Google on page 1

D) Conclusion + next minimal fix (but don’t implement)
Based on the evidence, propose the smallest fix and where it belongs:
- UI payload emission
- Supervisor mission schema/normalizer
- SEARCH_PLACES executor arg parsing
- google-places client request construction
- pagination loop

HOW TO INVESTIGATE (DO THIS NOW)
1) Query DB for both run_ids:
   - agent_runs / runs table (whatever stores run meta)
   - decision_log rows
   - step payload_json for step_type=SEARCH_PLACES (or step_id search_places*)
   - artefacts for leads_list + any debug artefacts you added

2) Find and print:
   - The earliest place where `google_query_mode` should appear
   - The last place before calling Google where mode is finalized

3) Confirm if both runs used identical final searchQuery strings.
   - If yes: toggle path never executed.
   - If no: Google truly returned 1 for both, then pagination/bias is the next suspect.

OUTPUT FORMAT
- Use clear headings.
- No speculation; every claim must point to a specific payload field or logged line.
- End with “Most likely fault location” and “Minimum change required”.

DELIVERABLE
- Write report to: .local/investigation-google-query-toggle.md
- Print the report summary to the console.

REMINDER
DO NOT EDIT CODE. REPORT ONLY.