URGENT: Google Places API costs are £316/month (9,036 calls in 14 days, spikes to 2,955/day). 
Root cause: generatePlacesQueries() creates 3-5 search queries per run, each firing a $0.032 
Google Places Text Search call. Combined with up to 5 replans + an alt query, a single run 
can make 8-11 Places API calls.

Make these changes to server/supervisor/mission-executor.ts ONLY:

CHANGE 1 — Reduce MAX_QUERIES from 5 to 2.
Find:
  const MAX_QUERIES = 5;
Replace with:
  const MAX_QUERIES = 2;

CHANGE 2 — Increase PLACES_PER_QUERY from 10 to 20 (get more results per call, fewer calls needed).
Find:
  const PLACES_PER_QUERY = 10;
Replace with:
  const PLACES_PER_QUERY = 20;

CHANGE 3 — Cap MAX_REPLANS_DEFAULT from 5 to 2.
Find:
  const MAX_REPLANS_DEFAULT = 5;
Replace with:
  const MAX_REPLANS_DEFAULT = 2;

CHANGE 4 — In the generatePlacesQueries() function, update the prompt to request fewer queries.
Find this line in the prompt string:
  Generate 3-5 search queries that will find the best results in Google Places for this business type.
Replace with:
  Generate 1-2 search queries that will find the best results in Google Places for this business type.

In the same prompt, find:
  - Each query should use different keywords to maximise coverage
Replace with:
  - Use ONE primary query with the most specific terms. Only add a second if there is a genuinely different way users would search for this business type.

CHANGE 5 — Remove the suggested_approaches[1] alt query search that fires on first replan.
Find the block that starts with:
        if (replansUsed === 1 && intentNarrative && intentNarrative.suggested_approaches.length > 1) {
And comment out or delete the ENTIRE if block down to its closing brace (it fires an extra 
SEARCH_PLACES call for suggested_approaches[1] on every first replan — pure waste).

Replace the entire block with:
        // COST_FIX: Removed suggested_approaches[1] alt query — was firing an extra $0.032 Places call on every first replan

EXPECTED IMPACT:
- Before: up to 5 initial queries + 5 replans + 1 alt = 11 calls/run ($0.352)  
- After: up to 2 initial queries + 2 replans = 4 calls/run ($0.128)
- ~64% cost reduction immediately

Only modify server/supervisor/mission-executor.ts.
Do NOT change google-places.ts, action-executor.ts, plan-executor.ts, or any other files.