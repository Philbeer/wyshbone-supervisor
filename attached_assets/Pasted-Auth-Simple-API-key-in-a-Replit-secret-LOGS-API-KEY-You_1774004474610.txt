Auth: Simple API key in a Replit secret (LOGS_API_KEY). You set it once, I include it in the header on every web_fetch. No rotation, no OAuth, no fuss.
Logging: Real-time — log each stage as it completes. The killer reason: if a run fails halfway, batch-at-end loses everything before the crash, which is exactly when you need it most. Real-time means every completed stage is already persisted. It's only ~5-8 inserts per run, negligible overhead.
Here's your prompt:

Repo: wyshbone-supervisor
Context: We're adding a structured logging system so run logs persist in Supabase and are accessible via HTTP endpoint. This replaces the need to manually download/paste logs. Claude will read these logs directly via web_fetch.
Step 1 — Create the Supabase table. Run this SQL in the Supabase dashboard (or via the existing Supabase client if there's a migration pattern):
sqlCREATE TABLE run_logs (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  run_id text NOT NULL,
  timestamp timestamptz DEFAULT now(),
  query_text text,
  stage text NOT NULL,
  level text NOT NULL DEFAULT 'info',
  message text NOT NULL,
  metadata jsonb DEFAULT '{}'::jsonb
);

CREATE INDEX idx_run_logs_run_id ON run_logs(run_id);
CREATE INDEX idx_run_logs_timestamp ON run_logs(timestamp DESC);
Step 2 — Create a logging utility module (e.g. src/utils/run-logger.ts or wherever utils live). It should export:

logRunEvent(runId, { stage, level, message, metadata?, queryText? }) — inserts a single row into run_logs via the existing Supabase client. level defaults to 'info'. This should be fire-and-forget (don't await it in the hot path — use .then().catch() so a logging failure never blocks or crashes a run).

Step 3 — Instrument the run pipeline. Find the main orchestration flow (wherever stages like intent extraction, discovery, evidence gathering, BJ scoring are kicked off and completed). At the start of each run, log an info event with stage run_start that includes the query_text. At the completion of each major stage, log an info event with the stage name and useful metadata (e.g. candidate count after discovery, evidence counts, BJ verdicts summary). On errors, log a level: 'error' event with the error message and stack in metadata. At run end, log a run_complete or run_failed summary.
Don't over-instrument — stick to the major pipeline stages. We can add granularity later.
Step 4 — Add the API endpoint. Create a route (Express or whatever the existing router pattern is):

GET /api/logs — Returns the most recent 20 runs as summaries. Query run_logs, group by run_id, return for each: run_id, query_text (from the run_start entry), earliest timestamp as started_at, latest timestamp as ended_at, whether any level='error' entries exist. Ordered by most recent first. Optional query param ?limit=N.
GET /api/logs/:runId — Returns all log entries for that run_id, ordered by timestamp ascending.
Both endpoints require header x-api-key matching the Replit secret LOGS_API_KEY. If missing or wrong, return 401. Keep the check simple — just a string comparison.

Step 5 — Add the Replit secret. Create a secret called LOGS_API_KEY with value wyshbone-logs-2026 (or any string — Phil can change it later).
Important notes:

Don't create any new Supabase client/connection — reuse the existing one.
The logging utility must never throw or block the main pipeline. Wrap everything in try/catch.
Keep the endpoint responses as clean JSON — no HTML, no wrappers.

Work on the main branch only — do not create or switch branches. After completing this task, append a structured summary to AGENT_LOG.md (what changed, files modified, decisions made, what's next).