PROMPT: SUPERVISOR - FIX G6 (NONSENSE EXECUTION MUST CLARIFY/REFUSE)

We must fix Chat Gates G6: the system must NOT execute nonsense/unmeasurable queries like:
"find the best vibes near council things"

Current bad behaviour: it runs a search and treats "Council Things" as a real location.

Target behaviour: force clarification before any run:

Ask for a real location (city/town/postcode). Explicitly say the provided phrase isn’t a place we can search.

Ask what the subjective criterion means in measurable terms (offer examples like lively/cosy/quiet/music/aesthetic etc).

Implement a minimal production-grade gate in server/supervisor/clarify-gate.ts:

Add a detector function (name it clearly) that returns true when:

message contains subjective/unmeasurable terms: best vibes / vibes / nice / good atmosphere / cool / amazing / top / best / trendy / good scene / hidden gems etc
AND

message lacks a concrete location OR includes a “non-place” location phrase like "council things", "near council", "near the council", "near local authority", "government" etc.

When detector returns true, route to clarify_for_run even if hasSearchIntent is true.
This should generate clarify questions that explicitly request:

A concrete location

A measurable translation of the subjective intent

Ensure it does not accidentally turn the subjective phrase into a location slot.

Update clarify-session.ts only if needed to store this “subjective intent” as semanticConstraint, but DO NOT treat it as runnable until clarified.

Add unit tests covering:

"find the best vibes near council things" => clarify_for_run with two questions (location + meaning of vibes)

"find cafes in bristol" => still runnable (no false positives)

"find the best cafes in bristol" => should clarify only if "best" is used without measurable criteria OR keep runnable but add a clarification question for “best” (choose one approach and make it consistent)

"find pubs near the council in Leeds" => must ask what “near the council” means (distance? which council building?) and confirm Leeds is the location.

Keep changes scoped. Make sure existing tests pass. Add new tests.

Deliverables:

Code changes

Tests passing

Short note in replit.md describing the new gate and examples.