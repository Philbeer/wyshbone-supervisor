Prompt: Supervisor – Subjective Predicate Gate (Batch 1 completion)

Goal
Fix inconsistent handling of subjective predicates so that ALL vague terms (e.g. “nice”, “best”, “good”, “cool”, “great atmosphere”, “chill”) behave the same way and never allow execution without clarification.

Problem
Currently:

“best pubs” → correctly triggers CLARIFY

“nice bars” → sometimes executes immediately (FAIL)

This is inconsistent and breaks honesty guarantees.

Required behaviour
Any request containing a subjective, non-measurable adjective must:

set can_execute=false

require clarification for definition (what does “nice” mean?)

never execute until clarified

This must apply regardless of:

entity type (pubs, bars, cafes, dentists)

location presence

time predicates

Definition
Subjective terms include (non-exhaustive but principled):

best, nice, good, great, cool, amazing, lovely, decent

good atmosphere, vibes, cool vibe, trendy

quality terms without measurable anchors

Measurable terms that are allowed:

lively, quiet, late-night, family-friendly, live music, rooftop, dog-friendly

Implementation instructions

In the constraint extraction phase:

Introduce a subjective_predicate constraint type

If detected AND no measurable definition present:

verifiability = unverifiable

hardness = hard

can_execute = false

why_blocked = “This description is subjective and needs clarification.”

Ensure this happens before execution routing, not after.

Do NOT special-case individual words.

Use a category-based classifier (subjective vs measurable).

Prefer false positives over false negatives.

Clarify question must ask:

“What do you mean by ‘nice’?”

Offer examples (e.g. cosy, lively, upscale, traditional, live music).

Ensure compound constraints still work:

“nice pubs opened recently” → must clarify BOTH “nice” and time predicate.

Tests to add (mandatory)
Add unit tests proving:

“Find nice bars in Manchester” → CLARIFY

“Find best pubs in Leeds” → CLARIFY

“Find good cafes” → CLARIFY

“Find lively bars in Manchester” → RUN

“Find cosy pubs with live music” → RUN

“Find nice pubs with live music” → CLARIFY (nice unresolved)

Deliverables

Exact files changed

Test file updated

All tests passing

Brief note explaining the subjective vs measurable rule

Do NOT refactor unrelated code.
Do NOT add UI changes here.
This is a Supervisor-only logic fix.