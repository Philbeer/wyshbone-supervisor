You are in the Wyshbone Supervisor repo. I need a FULL report of the REAL Supervisor -> Tower judgement loop that works (proof v2 / simulate), and how it is supposed to work for chat delegated runs.

Deliver a report with these sections:

Supervisor task ingestion

Where does Supervisor read/claim supervisor_tasks?

What DB table and fields does it expect (run_id, client_request_id, payload, status)?

What polling/worker mechanism is used (cron, interval loop, queue)?

File paths + function names.

Execution pipeline (real working loop)
Trace the working execution pipeline in order:

task claimed

tools executed

step artefacts created

artefact_created AFR emitted

Tower called (endpoint and payload)

tower_judgement artefact created

run completion status written
Include exact function names and file paths for each step.

Artefacts and AFR outputs

List the artefact types Supervisor creates in the working loop

Show exactly where it calls createArtefact() and emits AFR activity

Show how it links artefacts to run_id + crid

Tower integration contract

Show the Tower client code in Supervisor (where base URL is, env vars)

Show the exact request body it sends to Tower

Show the expected Tower response shape

Show what Supervisor does after a verdict (ACCEPT/RETRY/CHANGE_PLAN/STOP) if that exists.

Why chat delegation would fail even if UI inserts supervisor_tasks
Answer explicitly:

If UI inserts supervisor_tasks correctly, what could still prevent Supervisor from running it?
Examples: status not set, missing fields, wrong queue name, mismatch crid/run_id, worker disabled, environment flags, different db connection, etc.
Back each hypothesis with code evidence.

Instrumentation checklist
Provide the exact DB rows and AFR events we should expect to see for a delegated chat run, in order, if Supervisor is working.

Output requirements:

Extremely detailed.

Include file paths, code snippets, and ordered sequences.

Start with “facts proven by code”.

End with: “If UI delegates correctly and Supervisor is running, Tower judgement should appear because ___”.

Do NOT implement changes yet. Only report.