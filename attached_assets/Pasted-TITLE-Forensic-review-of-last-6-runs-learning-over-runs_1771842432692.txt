TITLE: Forensic review of last 6 runs (learning-over-runs validation)

You are acting as a read-only investigator.
Do not modify code.
Do not propose fixes.
Do not optimise anything.

Your task is to inspect and report on the last 6 completed runs only, to determine whether learning-over-runs is actually happening or not.

SCOPE

Inspect only the most recent 6 completed runs

Ignore:

runs still executing

any runs older than those 6

Assume all 6 runs were triggered intentionally as repeated tests

FOR EACH RUN, REPORT THE FOLLOWING (FACTS ONLY)

For each of the 6 runs, capture:

Identifiers

canonicalRunId

conversationId (if visible)

Outcome

Final state: PASS / STOP / FAIL

Stop reason (if any)

Plan behaviour

Number of replans (plan v1 → vN)

Whether replans count changed compared to previous runs

Learning / policy evidence

Was a policy bundle applied? (YES / NO)

Which policy fields were read or applied (eg max_replans)

Any evidence of policy updates between runs

Whether later runs used different values than earlier ones

Artefacts

Artefact count

Artefact types present (eg step_result, policy_snapshot, diagnostic)

Whether a policy snapshot or learning artefact exists

Execution anomalies

Any run with:

zero artefacts

early termination without explanation

mismatch between chat result and AFR state

long idle / hanging phase

COMPARATIVE ANALYSIS (REQUIRED)

After listing the 6 runs individually, answer only these questions:

Did any run behave differently because of a previous run?

Did max_replans or stop behaviour change across runs?

Is there clear evidence of learning being read, applied, or updated?

If learning did NOT occur, is that because:

no update was triggered, or

update was triggered but not applied, or

update exists but is ignored?

Do not speculate beyond what is visible in logs, artefacts, or state.

OUTPUT FORMAT (STRICT)

Run 1:

canonicalRunId:

Outcome:

Replans:

Policy applied:

Policy values:

Artefacts:

Notes (verbatim facts only):

Repeat for Run 2 → Run 6.

Then:

Summary

Evidence of learning across runs: YES / NO

If YES: what changed, exactly

If NO: where the chain breaks (read / update / apply)

IMPORTANT CONSTRAINTS

Do not explain architecture

Do not suggest fixes

Do not editorialise

Do not optimise

Do not assume intent

This is a truth-finding exercise only.