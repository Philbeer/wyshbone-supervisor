In wyshbone-supervisor-Post-CC, the handleCreateMonitor function at server/supervisor.ts line 849 has two bugs that need fixing.
The UI now sends explicit fields in the supervisor task's request_data: { schedule: 'once'|'hourly'|'daily'|'weekly', source_run_id: <agent_run.id from the user's just-completed search>, user_message, conversation_id }. The handler currently ignores both source_run_id and schedule and tries to re-derive them — that's the bug.
Bug 1 — the handler ignores request_data.source_run_id. At lines 869-905 it queries agent_runs for the most recent 5 completed runs in the conversation, then iterates them looking for a mission_extraction artefact, falling back to delivery_summary. Replace that whole block with a direct lookup: query agent_runs where id = request_data.source_run_id. Then query artefacts where run_id = request_data.source_run_id and type = 'mission_extraction'. Keep the existing delivery_summary fallback for the case where mission_extraction is absent on that specific run, but scope it to run_id = request_data.source_run_id. If source_run_id is missing or doesn't match an agent_run owned by task.user_id, return the error message — do not fall back to "most recent".
Bug 2 — the handler parses schedule via regex on user_message at lines 962-966. Replace that with const schedule = request_data.schedule;. If the field is missing or not in ['once', 'hourly', 'daily', 'weekly'], return an error. The 'once' case also needs handling in the existing intervals map (currently only hourly/daily/weekly) — for 'once', set next_wake_at based on now + a small interval (or follow whatever the rest of the codebase uses for one-shot monitors), and set auto_deactivate_after_first_run: true in the monitor's config so the wake scheduler deactivates it after the first successful run.
Step 1 — read the file. Confirm:
(a) the request_data field names I described match what's in scope at line 849
(b) the agent_runs query you'll use (.eq('id', source_run_id) plus a user_id check)
(c) whether 'once' as a schedule already has handling elsewhere (search for 'once' as a string literal in supervisor.ts and the storage layer) or whether you'll need to add it
Report back on step 1 with line references before writing any code. Do not modify the UI repo. Do not write fallback logic to "most recent run" — the UI provides the run_id explicitly and we trust it.