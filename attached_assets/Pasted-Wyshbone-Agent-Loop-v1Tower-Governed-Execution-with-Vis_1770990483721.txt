Wyshbone Agent Loop v1Tower-Governed Execution with Visible Replanning1. Core Principle (Non-Negotiable)Wyshbone is agentic only if execution is governed by evaluation.Supervisor executesTower judges with authoritySupervisor must reactUI must prove it happenedIf Tower cannot stop, retry, or force a plan change, the system is orchestration, not an agent.2. Canonical Agent Loop (Source of Truth)This loop must exist end-to-end and be visible.User Goal Received
Example: “Find 30 pubs in Arundel”Supervisor Creates Plan v1
Explicit steps, explicit assumptionsSupervisor Executes Plan v1
Tools run asynchronouslyArtefacts Created
(leads_list, deep_research_result, etc.)Tower Evaluates Artefacts
Against explicit success criteriaTower Issues Verdict (Authoritative)
One of:ACCEPTRETRYCHANGE_PLANSTOPSupervisor Reacts (Mandatory)ACCEPT → Propose next actionRETRY → Re-execute same planCHANGE_PLAN → Create Plan v2STOP → Explain failure honestlyUI Renders ProofTower verdict badgeDelivered vs RequestedGaps + confidencePlan history (v1 → v2)No silent success. No invisible failure.3. What Tower Must Judge (Minimum)Tower evaluations are simple and explicit.InputsGoalSuccess criteriaArtefact(s)OutputsTowerVerdict { verdict: ACCEPT | RETRY | CHANGE_PLAN | STOP delivered: number | summary requested: number | summary gaps: string[] confidence: 0–100 rationale: string}Tower does not suggest tools.
Tower only judges outcomes.4. Supervisor Responsibilities (Changed)Supervisor is no longer just an executor.It must:Maintain RunStateTrack Plan versionsObey Tower verdictsGenerate Plan v2 when requiredPropose next actions after ACCEPTSupervisor cannot ignore Tower.5. UI Responsibilities (Proof Layer)The UI is thin, but it must make agency legible.Every run must show:What was askedWhat was deliveredWhat’s missingTower’s verdictWhether the agent retried or replannedIf a user can’t tell the system changed its mind, the feature doesn’t count.6. Explicit Non-Goals (V1)This is critical to avoid scope creep.V1 does not include:CRM syncingEmail sendingFollow-upsLearning across runsMid-flight user editsAutonomous outreachThose come after the loop is real.7. Definition of “Agentic Enough” (V1 Exit Criteria)Wyshbone is agentic when:A run can fail visiblyThe system explains whyThe system retries or replans without user re-promptingThe UI proves this happenedAn investor can watch a run and say
“Yes, it adapted”That’s the bar. Nothing else matters yet.8. Why This Solves Both CritiquesTower-driven loop gives real autonomyClaude’s feedback becomes visible consequences of the same loopNo duplicate systemsNo fake agenticityOne spine, many future features