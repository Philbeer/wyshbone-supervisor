You are fixing a specific Wyshbone Supervisor issue.

IMPORTANT
Do NOT do a broad refactor.
Do NOT redesign the planner.
Do NOT change Tower contracts unless absolutely necessary.
Do NOT touch unrelated UI code.

This task is narrowly focused:

Ensure that delivered leads carry real verification evidence before Tower judges final_delivery.

Problem observed in real AFR runs

Multiple runs are delivering leads successfully, but Tower is stopping or changing plan because the delivered leads do not carry verification data.

Observed pattern in AFR:
- delivered leads exist
- final_delivery artefact exists
- Tower verdict includes NO_EVIDENCE_PRESENT
- reason says: "none carry verification data — evidence check was not attempted"

This means the system is producing leads, but not attaching usable verification/evidence payloads to those delivered leads before Tower evaluates them.

Goal

For any strategy that implies verification or evidence gathering, the final delivered leads sent to Tower must include attached evidence fields in the shape Tower can judge.

We need to fix the handoff between:
discovery / website evidence / ranking / verification steps
and
final_delivery / Tower judgement

What to investigate first

Trace the exact code path for:
1. lead discovery
2. evidence gathering / website evidence / verification steps
3. final lead assembly
4. final_delivery artefact creation
5. Tower input construction

Find where evidence is being lost, dropped, or never attached.

Questions to answer in code before changing anything

1. Where are evidence items first created?
2. In what structure are they stored?
3. At what point are final delivered leads assembled?
4. Why do final delivered leads in AFR only contain fields like:
   name
   address
   placeId / entity_id
   website
   phone
   but not:
   verified
   evidence
   source_url
   quote
   extracted_quotes
   attribute_evidence
5. Is the evidence step genuinely not running, or is it running but not merged into the final lead objects?
6. Which strategies are affected?
   - discovery_then_website_evidence
   - discovery_then_rank
   - any others that should produce verification evidence

Required fix

Implement the minimum safe production-grade fix so that:

A. If evidence/verification has been performed for a lead, the final delivered lead passed to Tower includes:
- verified: boolean
- source_url: string if available
- evidence: string or string[] or structured evidence summary
and, where available:
- attribute_evidence: array of evidence artefacts
- quote / extracted_quotes / context snippet equivalent

B. If no evidence step was actually attempted, do NOT fabricate verification.
The lead should remain unverified honestly.

C. If ranking-only strategies are used, decide explicitly:
- either ranking results are allowed to remain unverified and clearly marked as such
- or a lightweight verification attachment step must run before final_delivery
Do not leave this ambiguous.

D. final_delivery and any Tower input builder must preserve attached evidence instead of stripping it.

Implementation expectations

1. Audit the code path and identify the exact drop point.
2. Fix the merge/handoff so evidence survives into final delivered leads.
3. Keep the existing architecture intact.
4. Add targeted logs around:
   - evidence created count
   - leads with evidence count
   - leads without evidence count
   - final Tower payload sample shape
5. Add an AFR artefact or diagnostic artefact showing:
   verification_attachment_summary
with fields like:
   total_delivered
   with_verification_data
   without_verification_data
   evidence_sources_attached
This is for debugging only and must not alter behaviour.

Success criteria

After the fix, benchmark runs should no longer fail with NO_EVIDENCE_PRESENT when evidence was actually gathered.

Expected result:
- Tower can judge real evidence on delivered leads
- runs that truly have evidence should stop failing for “evidence check was not attempted”
- remaining failures, if any, should reveal the next real bottleneck

Deliverables

1. Brief root-cause explanation
2. Exact files changed
3. Summary of the data flow before vs after
4. Any edge cases left unresolved

Keep the fix tight, production-grade, and limited to this evidence attachment problem.