You are working in the Wyshbone UI repo. UI-only fix. Do NOT change any backend code.
Root cause (confirmed by investigation)
"On it — searching now" is persisted to Supabase. The message poller re-adds it from the DB every 3 seconds, overriding any state-level removal. The ONLY reliable fix is at the render layer — hide the message visually without removing it from state.
The activity ticker breadcrumbs (Thinking/Google Places/Run complete) come from a <LiveActivityTicker> component that self-fetches. The isActivityTicker message in state is the mount hook. The clarifyReceivedRef gate should suppress it in specialMessages, but as a belt-and-suspenders approach, also hide at the render layer.
Fix: Add render-layer suppression in client/src/pages/chat.tsx
Find the render filter that builds the visible message list. It's the .filter() call that checks chatMessage.hidden, empty content, etc. — around line 3138-3189 (the block that returns false to exclude messages from rendering).
Add these conditions INSIDE that filter, after the if (chatMessage.hidden) return false; line:
typescript// Hide "On it — searching now" and ticker during clarification
if (clarifyReceivedRef.current) {
  // Hide the SSE ack message ("On it — searching now") — it's misleading during clarification
  if (chatMessage.role === 'assistant' && !chatMessage.source && chatMessage.content &&
      /on it|searching now/i.test(chatMessage.content)) return false;
  // Hide activity ticker — the preflight probe breadcrumbs are confusing before a clarify question
  if ((chatMessage as any).isActivityTicker) return false;
  // Hide confidence line
  if ((chatMessage as any).isConfidence) return false;
}
This suppresses these items visually when clarification is active, without fighting the poller. The messages stay in state (so the poller doesn't create churn) but are simply not rendered.
Also verify: clarifyReceivedRef is set correctly
The investigation confirmed the ref exists and is wired. But verify in the Supabase realtime handler that isClarifyMessage actually evaluates to true. Open browser console during a clarify flow and check for the log:
🛡️ Supervisor bubble suppressed
vs the clarify message appearing. If the suppression log appears for the clarify message, that means isClarifyMessage is false and the metadata check needs updating. Check what metadata the Supervisor actually sends.
Add a temporary console.log right before the isClarifyMessage check:
typescriptconsole.log('[CLARIFY_DEBUG] metadata:', JSON.stringify(supervisorMessage.metadata), 'isClarify:', !!(supervisorMessage.metadata?.clarify_gate || supervisorMessage.metadata?.mode === 'clarify'));
Files to change

client/src/pages/chat.tsx — add render-layer suppression using clarifyReceivedRef, add debug log for metadata check