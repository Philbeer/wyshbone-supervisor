You are working in the Wyshbone Supervisor repo. One fix.
Problem: PASS3_CLARIFY fires LLM-generated questions instead of the preflight clarify probe
In server/supervisor.ts, find the PASS3_CLARIFY block inside processChatTask. It's the section that checks missionResult?.intentNarrative?.clarification_needed. There's a guard that skips PASS3_CLARIFY when the preflight probe would fire, but it only checks if entity OR location are missing. When the mission extractor finds a generic location from context (like "UK"), both fields are present, so the guard doesn't skip — and PASS3_CLARIFY posts the LLM's generic clarification question instead of letting the preflight probe ask about location.
Fix: Disable PASS3_CLARIFY entirely
The preflight clarify probe generates deterministic, well-structured questions. PASS3_CLARIFY generates non-deterministic LLM questions that are often unhelpful ("I'd be happy to find leads for you! Could you tell me what type of businesses you're looking for?").
Comment out or remove the entire PASS3_CLARIFY block. Find this code:
typescriptif (missionMode === 'active' && missionResult?.intentNarrative?.clarification_needed && !_missionHasEnoughToSearch && !missionQueryId) {
  const _preflightWouldFire = !_clarifyGateEntity || !_clarifyGateLocation;
  if (_preflightWouldFire) {
    console.log(`[PASS3_CLARIFY] Skipped — preflight probe owns this case...`);
  } else {
    const clarifyQ = missionResult.intentNarrative.clarification_question || 'Could you clarify your request a bit more?';
    // ... several lines posting to Supabase and returning ...
  }
}
Replace the ENTIRE block with a single log line:
typescriptif (missionMode === 'active' && missionResult?.intentNarrative?.clarification_needed && !_missionHasEnoughToSearch && !missionQueryId) {
  console.log(`[PASS3_CLARIFY] DISABLED — preflight probe is the sole clarification authority. clarification_needed=${missionResult.intentNarrative.clarification_needed} entity="${_clarifyGateEntity}" location="${_clarifyGateLocation}"`);
}
This ensures the preflight probe is always the one that asks clarifying questions. PASS3_CLARIFY never fires.
Verify
bashgrep -n "PASS3_CLARIFY" server/supervisor.ts | head -10
The output should show only the DISABLED log line and the earlier benchmark bypass log. No clarify message posting code should remain.
IMPORTANT: Save your response
Append a full summary with date header to AGENT_LOG.md in the workspace root.