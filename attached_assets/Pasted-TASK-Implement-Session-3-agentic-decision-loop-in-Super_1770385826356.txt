TASK: Implement Session 3 “agentic decision loop” in Supervisor using Tower Judgement API (now confirmed working).

Context: Tower endpoint is live at ${process.env.TOWER_URL}/api/tower/evaluate and expects:

top-level fields: run_id, mission_type, success, snapshot

success must include: target_leads, max_cost_per_lead_gbp, max_cost_gbp, max_steps, min_quality_score, stall_window_steps, stall_min_delta_leads (and max_failures if supported in Supervisor; Tower may ignore unknown fields).

Tower returns { verdict, reason_code, explanation, evaluated_at }
Important: TOWER_URL may have a trailing slash; when constructing endpoint URL, always strip it to avoid 301 redirect that can drop POST body.

Goal: During real job/mission execution (leadgen/search places), Supervisor must call Tower periodically (after each step) and obey verdicts, especially STOP. Must emit real AFR events:

judgement_requested

judgement_received

job_halted_by_judgement

judgement_failed (if call fails)

Implementation requirements:

Locate the real execution loop for leadgen/search (the code path that emits job_started/job_progress/job_completed AFR events).

Add a small run summary object stored in the job context (in-memory) and updated each step:

steps_completed

leads_found

leads_new_last_window (sliding window of last stall_window_steps)

failures_count

total_cost_gbp (or if no GBP cost exists, use a stable numeric “cost_units” but map it into total_cost_gbp as units; keep consistent)

avg_quality_score (if no model exists, proxy: valid lead = 1, invalid = 0; average)

Define default success thresholds for mission_type="leadgen" inside Supervisor (hardcode for now, but keep them grouped in one place). Make them demo-friendly:

low enough max_cost or strict cpl so we reliably trigger STOP in a demo.

Implement Tower call helper:

sanitize base: towerBase = process.env.TOWER_URL.replace(/\/+$/, "")

endpoint: ${towerBase}/api/tower/evaluate

POST JSON with exact shape Tower expects: { run_id, mission_type, success, snapshot }

AFR events:

BEFORE call: emit judgement_requested including success + snapshot

AFTER success: emit judgement_received including verdict/reason_code/explanation/evaluated_at + snapshot

If verdict is STOP: emit job_halted_by_judgement (include reason_code + explanation), then halt execution immediately:

stop further steps

mark job state as halted (distinct from completed/success)

ensure locks/overlap guards release cleanly

If verdict is CONTINUE: continue as normal

If call fails/unreachable: emit judgement_failed and default to CONTINUE (log loudly but do not crash the job)

Keep changes minimal and production-grade:

no secrets in logs

judgement loop must run in the real executor (not UI, not import side effects)

do not fake AFR events; only emit when call actually happens / verdict actually received.

Deliverables:

Code changes committed.

A demo run produces AFR timeline: job_started → progress → judgement_received STOP → job_halted_by_judgement (and job ends early).

Add a tiny integration test script in Supervisor (optional) that calls Tower using env URL (similar to the connectivity check), but the main proof is the halted run in AFR.

IMPORTANT: rewrite full files when editing; do not give partial diffs.