MODE: INVESTIGATE AND FIX.
Problem: Legacy clarification sometimes fires instead of preflight clarify probe
When the user sends "i'm looking for companies that buy cardboard wholesale", SOMETIMES instead of the preflight clarify probe (which correctly asks "What location should I search in?"), a DIFFERENT Supervisor message appears: "I'd be happy to find leads for you! Could you tell me what type of businesses you're looking for?"
The preflight clarify probe should ALWAYS intercept this query (location is missing), so this legacy path should never fire for lead-finding queries.
Investigate:

Find where the message "I'd be happy to find leads" or "what type of businesses" could be generated:

bashgrep -rn "happy to find\|what type of\|could you tell\|type of businesses" server/ --include="*.ts" | head -20

Check if there's a legacy clarification path that runs BEFORE or INSTEAD OF the preflight clarify probe:

bashgrep -rn "clarif" server/supervisor.ts | head -30
grep -rn "ask_user\|need_more_info\|missing_info" server/ --include="*.ts" | head -20

Check the constraint-gate and mission classifier — is there a path where the Supervisor decides to ask its own clarification question by posting a message to Supabase?

bashgrep -rn "saveMessage\|insertMessage\|messages.*insert" server/supervisor.ts | head -20

Check if the LLM used in the Supervisor (for mission extraction or classification) is generating this text. The message sounds like an LLM response. Find where the Supervisor uses an LLM to generate user-facing text:

bashgrep -rn "openai\|anthropic\|chat.*completion\|llm.*response\|generate.*message" server/supervisor.ts | head -20

Check evaluatePreflightClarify — under what conditions does it NOT fire? If the query contains "companies" and "cardboard", the mission extraction should detect entity but miss location, triggering the probe. Is there a race condition or early return that prevents the probe?

bashgrep -n -A 30 "evaluatePreflightClarify" server/supervisor.ts | head -40

Check processChatTask — is there an early classifier step that generates a response BEFORE the preflight probe runs?

bashgrep -n -A 20 "processChatTask" server/supervisor.ts | head -30
Fix:
If there IS a legacy clarification path, either:

Remove it entirely (if the preflight probe fully replaces it), OR
Add a guard so it never fires for lead-finding queries (if it serves other purposes)

If the preflight probe is being skipped for some queries, fix the skip condition.
Output

All grep output
The exact code path that generates "I'd be happy to find leads"
Why the preflight probe didn't fire for this query
The fix applied

IMPORTANT: Save your response
Append a full summary with date header to AGENT_LOG.md in the workspace root.