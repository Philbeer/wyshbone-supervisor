The Concatenation Guard in client/src/pages/chat.tsx is firing false positives on legitimate query refinements (e.g. "find pubs in arundel" → "find pubs in arundel that have live music"). Per the project's design philosophy (LLM-with-context, no rules engines or sticky flags), this substring-based check should be removed entirely. The LLM in the conversation router already handles dupes contextually.
Step 1 — Read all eight reference sites first.
Open client/src/pages/chat.tsx. Show me the exact current state of these lines (~5 lines of context each):

Line 545 (the useState)
Line 641 (cleanup call)
Line 3241 (cleanup call)
Line 3315 (cleanup call)
Lines 4016–4030 (the guard logic and trigger)
Line 4071 (the lastSentQueryRef.current = messageContent line — DO NOT remove this, it's used elsewhere)
Lines 5515–5535 (the JSX warning bar rendering)

Step 2 — Make these surgical removals only.
a. Delete the useState declaration at line 545:
tsconst [concatenationError, setConcatenationError] = useState<string | null>(null);
b. Delete the entire if block in handleSend (around lines 4016–4030) that contains the substring check and the setConcatenationError(...) call. This is the actual guard. Remove the whole block including its braces.
c. Delete the four cleanup calls — at lines 641, 3241, 3315, and any other setConcatenationError(null); line. Show me each one before deleting.
d. Delete the JSX block (around lines 5515–5535) that renders the yellow warning bar with the "Start fresh" and "Send anyway" buttons. This whole conditional render block goes.
e. Critical — preserve these:

lastSentQueryRef (the ref itself) — still used by the thinking indicator at lines 5393/5396 and elsewhere
The line lastSentQueryRef.current = messageContent near line 4071 — keep this
All other lines around the deletions

Step 3 — Verify clean removal.
Run:
bashgrep -n "concatenationError\|setConcatenationError\|Concatenation Guard\|may include your previous request" client/src/pages/chat.tsx
It must return zero matches. Show me the empty result.
Also confirm lastSentQueryRef still has the same usages it had before (it should appear ~24 times still).
Step 4 — Update replit.md.
In replit.md, find the line that documents "Concatenation Guard (A)" (around line 237). Remove that bullet entirely, since the guard no longer exists.
Step 5 — Show me the diff for chat.tsx (lines around the four deletion sites) and replit.md. Don't restart yet — I want to review.
Step 6 — After I confirm, restart the workflow cleanly (kill node, port free, fresh start, no EADDRINUSE) and reply with "ready — concatenation guard removed, single process running".
I'll then run a refinement query to confirm the warning no longer fires.