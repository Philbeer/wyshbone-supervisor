## Task: Add single verdict field to EvidenceResult — set it once after evidence gathering

This is Part 1 of a simplification to replace the overlapping evidenceStrength / match_valid / verified / verification_status checks with a single verdict per lead.

### What to do

In server/supervisor/mission-executor.ts, make these changes:

### Change 1 — Add verdict to EvidenceResult interface

Find the EvidenceResult interface (near the top of the file, around line 90). Add a new field at the end:
verdict: 'verified' | 'plausible' | 'no_evidence';

So the interface becomes:
```typescript
interface EvidenceResult {
  leadIndex: number;
  leadName: string;
  leadPlaceId: string;
  constraintField: string;
  constraintValue: string;
  constraintType: string;
  evidenceFound: boolean;
  evidenceStrength: 'strong' | 'weak' | 'none';
  isBotBlocked: boolean;
  towerStatus: TowerSemanticStatus | null;
  towerConfidence: number | null;
  towerReasoning: string | null;
  sourceUrl: string | null;
  snippets: string[];
  sourceTier?: 'first_party' | 'web_search_fallback' | 'snippet';
  verdict: 'verified' | 'plausible' | 'no_evidence';
}
```

### Change 2 — Set verdict in processOneLead

Inside the processOneLead function, find where evidenceStrength is assigned (the block that starts with `const evidenceStrength: 'strong' | 'weak' | 'none' =`). Immediately AFTER the evidenceStrength assignment, add:
```typescript
const verdict: 'verified' | 'plausible' | 'no_evidence' =
  towerStatus === 'verified' ? 'verified' :
  towerStatus === 'weak_match' ? 'plausible' :
  (towerStatus === 'no_evidence' || towerStatus === 'insufficient_evidence') ? 'no_evidence' :
  keywordFound ? 'plausible' :
  'no_evidence';
```

Then find the evidenceResults.push call that follows (the one inside processOneLead that pushes the per-constraint evidence). Add `verdict,` to the object being pushed. It currently has fields like leadIndex, leadName, etc. Add verdict after the snippets line:
snippets: extractedQuotes,
verdict,

### Change 3 — Set verdict in GPT-4o fallback verified block

Find the GPT-4o fallback section (search for GPT4O_FALLBACK). Inside the block where a verified result is processed (the `if (!hasContradiction)` block that sets `er.evidenceStrength = 'strong'`), add after the existing lines:
```typescript
er.verdict = 'verified';
```

### Change 4 — Set verdict in GPT-4o fallback non-verified paths

In the same GPT-4o fallback section, there are three other paths:
- The `if (hasContradiction)` block — add: `er.verdict = 'no_evidence';`
- The `else if (fbUpper.startsWith('CONTRADICTED'))` block — add: `er.verdict = 'no_evidence';`
- The final `else` block (unverified) — add: `er.verdict = 'no_evidence';`

Also in the error catch block at the end of the fallback try/catch, add: `er.verdict = 'no_evidence';`

### Change 5 — Set verdict for field match evidence

Find where FILTER_FIELDS pushes evidence results (search for `Field match:` in the snippets). These are direct field matches (name contains X). Add `verdict: 'verified' as const,` to each evidenceResults.push call in the FILTER_FIELDS section. There are two places — one in the main FILTER_FIELDS block and one in the replan FILTER_FIELDS block. Both should get `verdict: 'verified' as const,`.

### Change 6 — Set verdict for ranking evidence

Find where RANK_SCORE pushes evidence results (search for `Ranked by Google Places relevance`). Add `verdict: 'plausible' as const,` to the push call.

### What this does

Every EvidenceResult now has a verdict field set exactly once at the point where the evidence quality is known. Nothing downstream needs to recalculate it. In the next prompt, we'll use this verdict to replace the hard evidence filter, match_valid, and verification_status.

### What NOT to change

Do NOT remove evidenceStrength, evidenceFound, match_valid, or any existing fields yet. This prompt only ADDS the verdict field. The existing logic continues to work exactly as before. We remove the redundant fields in the next prompt.

Work on the main branch only — do not create or switch branches. After completing this task, append a structured summary to AGENT_LOG.md (what changed, files modified, decisions made, what's next).