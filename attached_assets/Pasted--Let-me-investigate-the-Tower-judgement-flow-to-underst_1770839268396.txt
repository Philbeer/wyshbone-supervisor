
Let me investigate the Tower judgement flow to understand where it breaks down.

Now let me check if there's any auto-trigger for Tower judgement when a run completes:

Now let me look at what happens after a run completes — specifically the Supervisor delegation flow and whether there's any auto-trigger:

Here's what I found from the investigation:

The issue is a gap between both repos — but the fix belongs in the Supervisor repo.

Here's the breakdown:

What happens now:

You send a chat message → the UI backend creates a run and delegates it to the Supervisor
The Supervisor completes the job and writes artefacts to Supabase
The UI sees "Run completed" via polling
Nobody automatically triggers Tower judgement — it just stops there
Why judgement doesn't happen automatically:

The /api/supervisor/request-judgement endpoint in this UI repo only fires when you manually click the "Request judgement" button
The SUPERVISOR_EXECUTION_ENABLED env var is set, but no code in this repo reads it to auto-trigger anything
The Supervisor service completes the job but doesn't call Tower or call back to the UI's Tower webhook (/api/tower/webhook/evaluation)
Where the fix should go:

Supervisor repo — When the Supervisor finishes a job, it should either:

Call Tower directly to get a judgement, then push the verdict back via the UI's webhook at /api/tower/webhook/evaluation, OR
Call this UI repo's /api/supervisor/request-judgement endpoint automatically after job completion
The UI repo already has the receiving infrastructure:

/api/tower/webhook/evaluation — ready to receive verdicts from Tower/Supervisor
/api/supervisor/request-judgement — ready to generate a judgement on demand
The Live Activity panel polls and displays judgements correctly once they exist
In short: The Supervisor finishes the job but never tells anyone to evaluate it. That "after job completion, trigger Tower" step is missing from the Supervisor's workflow. This UI repo is ready to receive and display the judgement — it's just never getting called.

Would you like me to do anything on this side, or is this enough to take to the Supervisor repo?