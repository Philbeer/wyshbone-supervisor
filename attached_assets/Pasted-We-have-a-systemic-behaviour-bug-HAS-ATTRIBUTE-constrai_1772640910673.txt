We have a systemic behaviour bug: HAS_ATTRIBUTE constraints (eg “live music”, “beer garden”, “dog friendly”) are extracted or implied by the user but currently do not change results. Two runs:

“Find pubs in Arundel”

“Find pubs in Arundel that say they have live music on their website”
produce identical delivered leads and identical receipt counts.

Implement attribute verification using existing WEB_VISIT text so HAS_ATTRIBUTE can become yes/unknown (and optionally no).

Constraints:

No new external APIs.

Use existing WEB_VISIT output text/content for each lead website.

Keep honest: do not mark YES without an evidence snippet + source URL.

Prefer UNKNOWN over NO unless you have strong negative evidence.

Required:

Detect when the goal includes HAS_ATTRIBUTE (live music) and the user explicitly says “on their website”.

Treat this as “verify via website” automatically. Do not trigger proxy clarification.

Build attributeEvidence keyed by place_id:
attributeEvidence[placeId][attribute] = { status: "yes"|"unknown"|"no", snippet?: string, url?: string }

Add an artefact type: attribute_verification
containing per-lead results (place_id, attribute, status, snippet, url).

Update CVL HAS_ATTRIBUTE verification to consult attributeEvidence so hard attribute constraints can be satisfied.

Update delivery summary exact/closest so:

hard HAS_ATTRIBUTE = unknown -> NOT exact

hard HAS_ATTRIBUTE = yes -> can be exact

Matcher:

Start minimal for live music:
“live music”, “live bands”, “music night”, “gig”, “open mic”
Return YES only if one of these appears in visited page text, with snippet and URL.

Acceptance:

After this change, the “live music on their website” run must no longer be identical to the plain run:
It must produce at least some “verified yes” leads if evidence exists, or clearly show UNKNOWN and not pretend.

attribute_verification artefact must appear in AFR for the run.

Output:

files changed

where attributeEvidence is built

where CVL reads it

sample attribute_verification payload