You are Wyshbone Supervisor. Report first, 
then make changes.

The Google Places search currently uses a single 
query string built mechanically from entity_category. 
This produces poor results because Google Places 
doesn't understand niche entity types like 
"bottle shop" and returns noisy mixed results.

The intent_narrative now contains:
- entity_description (richer than entity_category)
- suggested_approaches[] (ordered search strategies)
- fallback_intent (proxy if primary fails)
- key_discriminator (what separates right from wrong)
- entity_exclusions[] (what to avoid)

Report first:
1. Where exactly is the Google Places textQuery 
   string currently constructed? Paste the exact 
   code and the line where textQuery is set.

2. Is there any existing multi-query or retry 
   logic in the search step?

3. How are duplicate places currently handled 
   if the same place_id appears twice?

Then make these changes:

1. ADD A QUERY GENERATION STEP before Places search.
   
   Make a single gpt-4o-mini call to generate 
   3-5 optimised Google Places search queries 
   from the intent_narrative:

   Prompt:
   "You are a Google Places search expert. 
   Generate 3-5 search queries that will find 
   the best results in Google Places for this 
   business type.
   
   Entity the user wants:
   {entity_description}
   
   Key discriminator (what makes a result correct):
   {key_discriminator}
   
   Things to avoid:
   {entity_exclusions}
   
   Suggested approaches:
   {suggested_approaches}
   
   Rules:
   - Each query should use different keywords 
     to maximise coverage
   - Prefer specific terms over generic ones
   - Google Places works best with short 
     noun phrases like 'craft beer shop' or 
     'independent off licence'
   - Do not include location — that is added 
     separately
   - Order queries from most specific to 
     least specific
   
   Respond with JSON only:
   {
     'queries': [
       {
         'query': 'craft beer bottle shop',
         'rationale': 'most specific term'
       },
       ...
     ]
   }"

2. RUN ALL QUERIES against Google Places.
   
   For each generated query, run a Places 
   search with the same location bias.
   Cap at 5 queries maximum, 10 results each.
   
   Deduplicate by place_id — if the same 
   place appears in multiple query results, 
   keep it once and note how many queries 
   matched it (higher match count = stronger 
   candidate signal).

3. RANK CANDIDATES before website visits.
   
   Sort the deduplicated candidates by:
   1. match_count (appeared in most queries first)
   2. Then alphabetical as tiebreaker
   
   Take the top N candidates for website 
   visits where N = requested_count * 2 
   (to allow for exclusion filtering).

4. LOG the queries used:
   [PLACES] Generated {n} queries for 
   "{entity_description}":
   [PLACES] Query 1: "{query}" → {n} results
   [PLACES] Query 2: "{query}" → {n} results
   [PLACES] After dedup: {n} unique candidates
   [PLACES] After exclusion filter: {n} kept

5. Persist the generated queries as an 
   artefact of type search_query_plan so 
   it is visible in the AFR.

Show before and after for the bottle shop 
query — what single query was used before 
vs what 3-5 queries will be used after.