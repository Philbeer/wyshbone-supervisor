You’ve just implemented a generic HAS_ATTRIBUTE verification framework using an attribute map.

Before moving on, I need you to verify and harden it as a general mechanism, not a live-music fix.

1. Prove it is generic

Confirm explicitly (in text, not code comments):

The code path for attribute verification is the same for all HAS_ATTRIBUTE constraints

No logic branches on specific attribute names like “live_music”

The only per-attribute variation lives in the ATTRIBUTE_SIGNALS config map

If any logic is attribute-specific, refactor it now.

2. Add a “no over-assertion” safety rule

Add the following invariant to the verifier:

A HAS_ATTRIBUTE constraint may only be marked yes if at least one explicit positive keyword is present in the evidence quote.

No semantic guessing.
No inference from vibes.
If the keyword isn’t present, verdict must be unknown.

Confirm this is enforced.

3. Add a structured UNKNOWN reason

When verdict is unknown, ensure the artefact includes a clear reason enum, eg:

no_relevant_pages_found

pages_crawled_no_keywords

official_site_blocked

only_weak_third_party_mentions

This is required so Tower and UI can explain why something wasn’t verified.

4. Dry-run two different attributes (thought exercise)

Without running code, reason through these two inputs and explain what your system would output:

A)
“Find pubs in Arundel that allow dogs”
Evidence contains: “well behaved dogs welcome in the bar”

B)
“Find pubs in Arundel with a beer garden”
Evidence contains: “outdoor seating available” but no phrase “beer garden”

For each:

verdict

confidence

why

If your answers differ from expected conservative behaviour, fix the logic.

5. Lock MVP scope

Confirm that:

Only the seeded attributes are active (live_music, dog_friendly, beer_garden)

Any unknown attribute key defaults to unknown with reason “unsupported_attribute”

No new attributes auto-activate without config

6. Report back

Reply with:

Confirmation the mechanism is generic

Any fixes applied

One paragraph summary of how HAS_ATTRIBUTE verification now works, in plain English

Do not add new features.
This is a hardening pass only.