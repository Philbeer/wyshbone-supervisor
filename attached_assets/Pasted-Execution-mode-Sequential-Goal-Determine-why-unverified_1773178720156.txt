Execution mode: Sequential

Goal
Determine why unverified leads are still being delivered even though the hard evidence filter was fixed.

Context

The hard evidence filter was patched:

if (!leadsChecked.has(i)) return false;

The outer guard was also removed so the filter always runs when hard evidence constraints exist.

Unit tests pass.

However, the AFR run still shows:

Delivered: 5 leads
Verified: 3 leads

Meaning 2 leads were delivered without verification data.

This means the real runtime path is still allowing unverified leads through.

Your task is to trace the exact runtime pipeline and find where the filter is being bypassed.

Investigation steps

1. Trace the full pipeline from discovery to delivery.

Follow the data flow for leads:

SEARCH_PLACES
→ candidate leads
→ enrichment / evidence collection
→ leadsChecked population
→ applyHardEvidenceFilter()
→ final delivery list
→ artefact creation (lead_pack / delivery summary)

2. Confirm exactly where applyHardEvidenceFilter() is called.

Record:
- file
- function
- line number

3. Identify the list that the filter operates on.

For example:

candidateLeads
rankedLeads
filteredLeads
deliveredLeads

4. Identify the list used to create the final delivery artefacts.

Specifically check where these are produced:

lead_pack artefact
delivery_summary artefact
returned results

5. Determine if these lists are different objects.

Example failure pattern:

filteredLeads = applyHardEvidenceFilter(candidateLeads)

but delivery uses:

finalLeads = rankedLeads.slice(0, requested_count)

which would bypass the filter.

6. Confirm whether leads can be re-added after filtering due to:

ranking stage
top-N slicing
replanning
fallback delivery
legacy constraint pipeline

7. Compare:

IDs of leads entering the filter
IDs of leads leaving the filter
IDs of leads in final delivery

If the sets differ, identify exactly where the extra leads enter.

Deliverables

1. A clear explanation of why the runtime system still delivered 5 leads when only 3 were verified.

2. The exact code location where the filtered list diverges from the delivered list.

3. The minimal patch required to ensure:

The final delivered leads MUST be the output of applyHardEvidenceFilter().

4. Write findings to:

SUPERVISOR_DELIVERY_PATH_AUDIT.md