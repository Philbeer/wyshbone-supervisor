You are working in the WYSHBONE SUPERVISOR repo.

Goal (V1 decision):
REMOVE Google “business type / category” as a constraint entirely, for now, across all searches.
We will rely on Google Text Search for discovery and then use Wyshbone filtering later.
This must stop business_type from:
- triggering capability checks
- causing STOP for “unverifiable hard constraint”
- reducing search_budget
- influencing Tower “exact match” classification
- affecting verification_summary / delivery_summary

Scope:
Supervisor only. Do NOT change UI.
Do NOT add new tools. Do NOT add new panels/endpoints.
Implement the minimal, production-grade change with tests.

What to change (requirements):

1) Constraint extraction:
- When parsing the user query, DO NOT create a c_category / business_type constraint at all.
- Example: “find new micropubs in sussex”
  - “micropub” must be treated as a TEXT query term, not a category constraint.
- Same for “dentists”, “accountants”, etc. For now, NO business_type constraint of any kind.

2) Capability check:
- Since business_type constraint no longer exists, capability checks must never return blocking_hard: [c_category].
- Remove/disable any code paths that assume business_type is present.

3) Discovery:
- SEARCH_PLACES must be driven purely by text query + location context.
- Ensure the discovery search budget is sensible and never collapses to 1 because business_type/requested_count logic is absent.
  - If user did not provide a number, set a default internal target (e.g. 20 or 30) BUT label it as internal default, not user requested.
  - The user-facing requested_count_user should remain “any” if no number was provided.

4) Verification / CVL:
- Remove any verification logic that tries to “verify business type/category”.
- Verification_summary should not mention hard_unverifiable=micropubs or similar, because business type is no longer a constraint.
- Location constraints remain as-is (do not touch location in this change).

5) Tower STOP logic:
- Tower must NOT STOP due to “Unverifiable hard constraint: micropubs” or any business_type term.
- That stop_reason must be impossible after this change.

6) Delivery summary / exact vs closest:
- Remove/disable any classification that depends on original business_type/category constraint.
- The UI should no longer show “not verified” splits caused by business_type constraint checks.

Acceptance criteria (must be demonstrated with tests or run logs):
A) Query: “find new micropubs in sussex”
- Must NOT create c_category constraint
- Must NOT show capability check: blocking_hard c_category
- Must run SEARCH_PLACES with text query “micropub(s) sussex” (or equivalent)
- Must return MORE than 1 lead if Places returns them (no internal cap to 1)
- Must NOT STOP for “unverifiable hard constraint”
- Delivery summary should reflect delivered candidates without business_type gating

B) Query: “find dentists in sussex”
- Same: no business_type constraint, no category verification, sensible budget, no truncation to 1.

Implementation notes:
- Keep changes minimal and localized.
- If existing code heavily assumes a business_type constraint, refactor by making business_type optional and defaulting to “none”.
- Do not introduce new behaviour except removing business_type.

Deliverables:
- Code changes in supervisor (constraint extraction + downstream handling)
- Unit tests (or integration tests) covering A and B
- Short report describing exactly which files/functions were changed and why