BUG FIX — Clarification gate fix did not persist, needs to be reapplied and verified

PROBLEM: A fix was previously applied to the clarification gate in server/supervisor.ts to prevent the pipeline from halting on queries that have enough information to search. The fix added a _missionHasEnoughToSearch check that overrides the LLM's clarify_if_needed flag when entity_category, location_text, and at least one constraint are present.

However, the fix did not persist — the pipeline is still halting at pass3_clarify for the query "Find organisations that work with the local authority in Blackpool", which has:
- entity_category: "organisations that work with the local authority"
- location_text: "Blackpool"
- constraints: 1 (relationship type)

TASK:

1. First, read the current state of the clarification gate code in server/supervisor.ts. Look for the section around lines 1150-1170 where the pass3_clarify halt decision is made. Check whether the _missionHasEnoughToSearch guard is present or not.

2. If the guard is NOT present (the previous fix was lost): Re-apply it. Before the block that returns pass3_clarify, add a check:
   - Read entity_category (or entity_kind) from the mission/intent extraction result
   - Read location_text from the mission/intent extraction result
   - Read the constraints array
   - If entity_category is non-empty AND location_text is non-empty AND constraints.length > 0, then SKIP the clarification halt entirely — proceed as if clarify_if_needed was false
   - Log: console.log('[CLARIFY-GATE] Suppressed — mission has enough to search:', { entity_category, location_text, constraintCount })

3. If the guard IS present (the fix exists but isn't working): Add diagnostic logging to understand why it's not triggering:
   - console.log('[CLARIFY-GATE] Values:', { entity_category, location_text, constraintCount, clarify_if_needed })
   - console.log('[CLARIFY-GATE] _missionHasEnoughToSearch:', result)

4. After applying or diagnosing, RESTART the server explicitly (not just relying on hot-reload) and verify the fix is live by checking the startup logs.

5. Verify the fix persists: read back the file after saving to confirm the changes are actually written to disk.

DO NOT modify any other files. Only touch the clarification gate logic in server/supervisor.ts.

After completing, append findings to AGENT_LOG.md documenting: whether the previous fix was present or lost, what was done, and confirmation that the file was saved and server restarted.