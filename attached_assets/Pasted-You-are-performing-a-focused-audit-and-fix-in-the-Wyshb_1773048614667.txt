You are performing a focused audit and fix in the Wyshbone Supervisor.

IMPORTANT
Do NOT redesign the planner.
Do NOT refactor the architecture.
Do NOT change Tower contracts.

This task is narrowly focused on one problem:

Verification evidence is not being attached to delivered leads for some strategies.

Real benchmark runs show this pattern:

- leads are successfully discovered
- final_delivery artefact contains many leads
- but Tower reports NO_EVIDENCE_PRESENT or 0 verified
- some strategies attach evidence correctly, others attach none

Goal

Audit verification coverage by strategy and fix strategies that produce zero verification evidence.

-------------------------------------

PART 1 — Identify all execution strategies

Locate where strategy names are defined or selected.

Typical examples include:

- discovery_only
- discovery_then_rank
- discovery_then_website_evidence
- relationship_search
- other variants used in plan steps

Produce a list of all strategy types currently used.

-------------------------------------

PART 2 — Measure verification coverage by strategy

For each strategy type, inspect the run pipeline and determine:

1. whether verification or evidence extraction is attempted
2. where evidence objects are created
3. whether evidence is attached to lead objects
4. whether evidence survives into final_delivery
5. whether Tower receives evidence fields

Add diagnostic logging for each run:

verification_attachment_summary

Example log structure:

{
  strategy: "discovery_then_rank",
  leads_delivered: 20,
  leads_with_verification: 0,
  leads_without_verification: 20
}

This must appear in AFR or structured logs.

-------------------------------------

PART 3 — Identify zero-verification strategies

Find strategies where:

leads_with_verification == 0

These are the priority problems.

Typical suspect:

discovery_then_rank

-------------------------------------

PART 4 — Fix zero-verification strategies

For any strategy that currently produces no verification evidence:

Implement a minimal verification attachment step before final_delivery.

Options depending on strategy:

1. WEBSITE STRATEGIES
   Ensure website evidence extraction attaches:
   - verified
   - source_url
   - quote or extracted text

2. RANKING STRATEGIES
   Decide one of these approaches:

   OPTION A (preferred)
   Attach lightweight verification evidence from the ranking source
   (Google Places rating, review count, etc.)

   OPTION B
   Explicitly mark leads as unverified but do NOT let Tower treat them as missing evidence.

Choose the smallest safe change.

-------------------------------------

PART 5 — Preserve existing architecture

Do not:

- change the planner
- modify Tower evaluation logic
- alter constraint extraction

Only ensure evidence objects survive from extraction → lead objects → final_delivery.

-------------------------------------

PART 6 — Output report

After implementing diagnostics, provide:

1. list of all strategies
2. verification coverage for each
3. which strategies had 0 verification
4. files changed
5. summary of the fix

-------------------------------------

Success criteria

Benchmark runs should no longer produce:

NO_EVIDENCE_PRESENT

when leads were actually verified.

Tower should receive leads with attached evidence where appropriate.