Open server/supervisor/mission-executor.ts.

Add a new helper function and use it when building the final_delivery artefact. Do not touch any other artefact, file, or function.

CHANGE 1 — Add a helper function.

Find the existing helper functions near the top of the file (buildMatchEvidence, buildMatchBasis, buildSupportingEvidence, buildMatchSummary — they appear in that order). Immediately AFTER buildMatchSummary's closing brace and BEFORE the deriveSearchParams function, insert this new function:


interface ConstraintResult {
  constraint_id: string;
  constraint_type: string;
  constraint_field: string;
  constraint_value: string;
  hardness: 'hard' | 'soft';
  status: 'verified' | 'unverified' | 'not_attempted';
  total_checked: number;
  verified_count: number;
  unverified_count: number;
  sample_evidence: Array<{
    lead_name: string;
    quote: string | null;
    source_url: string | null;
    matched_phrase: string;
    verification_status: string;
  }>;
}

interface VerificationSummary {
  verified_exact_count: number;
  total_constraints: number;
  hard_constraints_satisfied: number;
  hard_constraints_total: number;
  constraint_results: ConstraintResult[];
}

function buildVerificationSummary(
  evidenceResults: EvidenceResult[],
  structuredConstraints: StructuredConstraintPayload[],
  deliveredLeads: Array<{ name: string; placeId: string }>,
): VerificationSummary {
  // Group evidence results by constraint key
  const byConstraint = new Map<string, EvidenceResult[]>();
  for (const er of evidenceResults) {
    const key = `${er.constraintType}::${er.constraintField}::${er.constraintValue}`;
    const arr = byConstraint.get(key) || [];
    arr.push(er);
    byConstraint.set(key, arr);
  }

  // Build one constraint_result per structured constraint
  const constraint_results: ConstraintResult[] = structuredConstraints.map((sc) => {
    // Match structured constraint to evidence group by (type, field, value)
    const key = `${sc.type}::${sc.field}::${String(sc.value ?? '')}`;
    const group = byConstraint.get(key) || [];

    // location_constraint is verified by directory data — no per-lead evidence needed;
    // every delivered lead implicitly satisfies it because location filtering already happened.
    if (sc.type === 'location_constraint') {
      return {
        constraint_id: sc.id,
        constraint_type: sc.type,
        constraint_field: sc.field,
        constraint_value: String(sc.value ?? ''),
        hardness: sc.hardness,
        status: 'verified',
        total_checked: deliveredLeads.length,
        verified_count: deliveredLeads.length,
        unverified_count: 0,
        sample_evidence: [],
      };
    }

    const verified_count = group.filter(g => g.evidenceFound).length;
    const total_checked = group.length;
    const unverified_count = total_checked - verified_count;

    const status: ConstraintResult['status'] =
      total_checked === 0 ? 'not_attempted' :
      verified_count > 0 ? 'verified' :
      'unverified';

    // Sample up to 3 verified evidence items for Tower to read
    const sample_evidence = group
      .filter(g => g.evidenceFound && g.snippets.length > 0)
      .slice(0, 3)
      .map(g => ({
        lead_name: g.leadName,
        quote: g.snippets[0]?.substring(0, 300) ?? null,
        source_url: g.sourceUrl,
        matched_phrase: g.constraintValue,
        verification_status: g.towerStatus === 'verified' ? 'verified'
          : g.towerStatus === 'weak_match' ? 'weak_match'
          : g.evidenceStrength === 'strong' ? 'proxy'
          : 'unverified',
      }));

    return {
      constraint_id: sc.id,
      constraint_type: sc.type,
      constraint_field: sc.field,
      constraint_value: String(sc.value ?? ''),
      hardness: sc.hardness,
      status,
      total_checked,
      verified_count,
      unverified_count,
      sample_evidence,
    };
  });

  // verified_exact_count = number of leads where every HARD constraint is verified.
  // A lead is "exact" if for each hard constraint, the lead either has verified evidence
  // OR the constraint is location_constraint (already filtered) OR text_compare (field match).
  const hardConstraintKeys = structuredConstraints
    .filter(sc => sc.hardness === 'hard')
    .map(sc => `${sc.type}::${sc.field}::${String(sc.value ?? '')}`);

  const verifiedLeadPlaceIds = new Set<string>();
  for (const lead of deliveredLeads) {
    let allHardSatisfied = true;
    for (const sc of structuredConstraints) {
      if (sc.hardness !== 'hard') continue;
      if (sc.type === 'location_constraint') continue; // implicit
      if (sc.type === 'text_compare') continue;        // already filtered at field level

      const key = `${sc.type}::${sc.field}::${String(sc.value ?? '')}`;
      const group = byConstraint.get(key) || [];
      const leadEvidence = group.find(g => g.leadPlaceId === lead.placeId);
      if (!leadEvidence || !leadEvidence.evidenceFound) {
        allHardSatisfied = false;
        break;
      }
    }
    if (allHardSatisfied) verifiedLeadPlaceIds.add(lead.placeId);
  }

  const hard_constraints_total = constraint_results.filter(cr => cr.hardness === 'hard').length;
  const hard_constraints_satisfied = constraint_results.filter(
    cr => cr.hardness === 'hard' && cr.status === 'verified',
  ).length;

  return {
    verified_exact_count: verifiedLeadPlaceIds.size,
    total_constraints: structuredConstraints.length,
    hard_constraints_satisfied,
    hard_constraints_total,
    constraint_results,
  };
}


CHANGE 2 — Use the helper when building the final_delivery artefact.

Find the createArtefact call that creates the final_delivery artefact. It is inside executeMissionDrivenPlan, near a console.log line that says "=== Phase: FINAL_DELIVERY ===". The createArtefact call has type: 'final_delivery' and the payload field includes leads, evidence_summary, delivered_count, verification_policy.

IMMEDIATELY BEFORE that createArtefact call, add:

  const verificationSummary = buildVerificationSummary(
    evidenceResults,
    structuredConstraints,
    leads.map(l => ({ name: l.name, placeId: l.placeId })),
  );
  console.log(`[MISSION_EXEC] Verification summary: ${verificationSummary.verified_exact_count} leads with all hard constraints verified, ${verificationSummary.hard_constraints_satisfied}/${verificationSummary.hard_constraints_total} hard constraints met`);

Then INSIDE the payload object of that createArtefact call, add this field (alongside the existing evidence_summary, delivered_count, verification_policy fields — do not remove or modify any existing fields):

  verification_summary: verificationSummary,

The same payload shape is used by the Tower at the /judge-artefact endpoint to read constraint_results[] and verified_exact_count. Keep evidence_summary too — both fields will coexist; different consumers read different fields.

CHANGE 3 — Apply the same change to the GPT-4o primary path.

Open server/supervisor/gpt4o-search.ts.

Find the createArtefact call that creates the final_delivery artefact (type: 'final_delivery'). Apply the same two additions:
1. Build verificationSummary using buildVerificationSummary (import it from mission-executor: add to the existing import block at the top of the file: `import { buildVerificationSummary, type ... } from './mission-executor';` — keep the existing imports). If buildVerificationSummary is not exported from mission-executor, add `export` to its declaration in mission-executor.ts.
2. Add verification_summary: verificationSummary to the payload.

Use the equivalent variables already in scope in gpt4o-search.ts for evidenceResults, structuredConstraints, and the delivered leads.

Do not commit. Run a syntax/typescript check. Report back with the line numbers of each change.