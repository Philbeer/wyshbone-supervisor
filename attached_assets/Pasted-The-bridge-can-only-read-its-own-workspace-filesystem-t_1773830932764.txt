The bridge can only read its own workspace filesystem (the MCP server), not the actual supervisor code. But I have everything I need from the AFR — the issue is clear.
From the AFR, the intent extractor outputs this:
json{
  "raw": "work with the local authority",
  "type": "relationship", 
  "hardness": "hard",
  "evidence_mode": "web_search",
  "clarify_if_needed": true,
  "clarify_question": "What specific types of organisations?"
}
```

Pass 3 sees `clarify_if_needed: true`, triggers the clarification gate, and the run halts. The first run (10:12) got lucky — gpt-4o-mini didn't set that flag. This run it did. Same input, different result.

Here's the prompt — paste into **Supervisor**:
```
BUG FIX — Non-deterministic clarification gate causing GP pipeline regression

PROBLEM: The same query "Find organisations that work with the local authority in Blackpool" sometimes executes successfully (producing 8 results) and sometimes stops at the clarification gate without searching. The cause is that gpt-4o-mini non-deterministically sets `clarify_if_needed: true` on relationship constraints. This means identical inputs produce different behaviour across runs, which is a regression — the pipeline should reliably proceed to search.

CONTEXT FROM AFR: The intent extractor returns a constraint with:
  - type: "relationship"
  - hardness: "hard"  
  - clarify_if_needed: true (SOMETIMES — this is the non-deterministic part)
  - clarify_question: "What specific types of organisations are you interested in?"

When clarify_if_needed is true, Pass 3 fires the clarification gate and the run halts to ask the user. When it's false (same query, different run), the pipeline proceeds normally and finds 8 good results.

WHAT TO FIX:

The clarification gate should NOT halt execution when the query already has enough information to produce useful results. The query "Find organisations that work with the local authority in Blackpool" has:
  - A clear entity type ("organisations")
  - A clear relationship constraint ("work with the local authority")
  - A clear location ("Blackpool")

That is enough to search. The system should search, deliver results with confidence levels, and THEN offer to narrow down if the user wants more specificity. Blocking the run to ask for clarification is the wrong UX — it makes the user do work the system should do.

THE FIX — find where the clarification gate decision is made (the code that reads clarify_if_needed from the intent extractor output and decides whether to halt execution). Apply this logic:

1. If ALL of these are present in the extracted intent, PROCEED without clarifying (override clarify_if_needed to false):
   - entity_kind or entity_category is not empty/null
   - location_text is not empty/null
   - At least one constraint exists (regardless of type)

2. Only halt for clarification when the query is genuinely too vague to search at all, for example:
   - No location specified AND no entity type specified
   - The query is something like "find stuff" with no actionable content

3. The LLM's clarify_if_needed flag should be treated as a SUGGESTION, not a gate. The deterministic check above overrides it. This eliminates the non-determinism — the same input always produces the same routing decision.

IMPORTANT: Do NOT modify the intent extractor prompt or the LLM call itself. The LLM can still output clarify_if_needed — we just don't let it be the sole decision-maker for halting execution. The fix is in the gate logic that READS the flag and decides whether to halt.

Also do NOT modify any other part of the pipeline — no changes to GP search, website visits, evidence extraction, Tower calls, or the new gpt4o_primary path.

FILES TO LOOK AT:
- Wherever Pass 3 / the clarification gate logic lives — this is the code that checks clarify_if_needed and decides to halt or proceed
- It may be in the mission executor, the supervisor orchestrator, or a dedicated clarification gate file
- Look for where the execution verdict "pass3_clarify" is returned — that's the code path to modify

EXPECTED OUTCOME: The query "Find organisations that work with the local authority in Blackpool" should ALWAYS proceed to search, never halt for clarification. It has entity type + location + constraint — that's enough.

After fixing, run the same query twice to verify deterministic behaviour, then append findings to AGENT_LOG.md docume