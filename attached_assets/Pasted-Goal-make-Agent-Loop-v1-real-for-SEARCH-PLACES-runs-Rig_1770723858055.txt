Goal: make Agent Loop v1 real for SEARCH_PLACES runs.

Right now runs are marked run_completed even when there is no tower_judgement artefact and the goal is missed (see: “find 150 pubs in kent” run shows run_completed + leads_list only).

Implement these exact changes:

A) HARD RULE: For SEARCH_PLACES runs, do NOT emit run_completed anywhere except inside the agent loop after Tower verdict ACCEPT. If Tower verdict is STOP, emit run_stopped. If verdict is CHANGE_PLAN or RETRY, emit replanning/continuing events and re-run, but still do not emit run_completed.

B) Find every place run_completed is emitted today (plan-executor, routes, afr logger, anywhere). Gate it:
- If tool is SEARCH_PLACES or the plan contains SEARCH_PLACES, do not emit run_completed in the old path.
- Replace with a neutral event like plan_execution_finished if you need something for UI progress, but not run_completed.

C) Ensure Tower judgement always happens for SEARCH_PLACES:
- After SEARCH_PLACES produces results, create leads_list artefact with delivered_count and target_count.
- Call Tower endpoint (use TOWER_URL) with { goal, success_criteria.target_count, artefact } and persist the response as a tower_judgement artefact.
- Then apply verdict: ACCEPT => run_completed + run_summary; STOP => run_stopped + run_summary; RETRY/CHANGE_PLAN => create plan_update (for change plan), rerun SEARCH_PLACES with adjusted args, create a NEW leads_list, re-judge, loop with max 1 retry and max planVersion 2.

D) Add very obvious logs for debugging:
- [AGENT_LOOP] tool=SEARCH_PLACES target= delivered=
- [TOWER_CALL] url= ok=true status=
- [TOWER_VERDICT] verdict= delivered= requested=

E) Fix routing/tool selection for the “pubs in Kent” style request:
- If user asks for pubs/bars/venues in a location and we are leadgen mode, route to SEARCH_PLACES (Google Places), not “Wyshbone Global Database”.
- Parse the requested number from the user message (150, 200 etc) and pass it as target_count (and as maxResults if you use that). Cap at 200 if you must.

Deliverable: when I run “find 150 pubs in kent”, I should see, in artefacts:
1) leads_list with target_count=150
2) tower_judgement with verdict (likely CHANGE_PLAN)
3) if verdict triggers rerun: plan_update + new leads_list + new tower_judgement
4) run only becomes “completed” after ACCEPT, otherwise “stopped” with a run_summary explaining the gap.

Make the smallest changes necessary and tell me exactly which files you changed.
