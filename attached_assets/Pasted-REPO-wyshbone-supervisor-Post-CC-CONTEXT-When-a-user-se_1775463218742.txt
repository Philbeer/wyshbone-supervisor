REPO: wyshbone-supervisor-Post-CC

CONTEXT: When a user searches "find bakeries in Arundel", Google Places returns results from Forest Row (30 miles away). The CVL's geo verification has bounding boxes for counties (West Sussex, Kent) but NOT for towns/cities (Arundel, Brighton). When it can't find a region match, it returns UNKNOWN instead of checking the lead's address. This means out-of-area leads slip through.

ROOT CAUSE: `verifyLocationGeo()` in `geo-regions.ts` returns UNKNOWN when `resolveRegionKeys("Arundel")` finds no matching region. It never falls back to checking the address string.

FIX: Add an address-substring fallback when no geo bounding box exists. If the lead's address doesn't contain the location name, mark it as OUT_OF_AREA.

═══════════════════════════════════════════════════════════════
CHANGE 1: server/supervisor/geo-regions.ts — verifyLocationGeo function
═══════════════════════════════════════════════════════════════

The function signature needs to accept the lead's address for fallback checking. Change the signature from:

```typescript
export function verifyLocationGeo(
  lat: number | null | undefined,
  lng: number | null | undefined,
  locationName: string,
  isHardConstraint: boolean,
): GeoVerificationResult {
```

To:

```typescript
export function verifyLocationGeo(
  lat: number | null | undefined,
  lng: number | null | undefined,
  locationName: string,
  isHardConstraint: boolean,
  leadAddress?: string | null,
): GeoVerificationResult {
```

Then, in the FIRST `if (regionKeys.length === 0)` block (where no region data exists), REPLACE the entire block with:

```typescript
  if (regionKeys.length === 0) {
    // No bounding box for this location (it's a town/city, not a county).
    // Fallback: check if the lead's address contains the location name.
    const locationLower = locationName.toLowerCase().trim();
    const addressLower = (leadAddress || '').toLowerCase();
    
    if (addressLower && locationLower) {
      // Check if address contains the location name
      const addressContainsLocation = addressLower.includes(locationLower);
      
      if (addressContainsLocation) {
        return {
          status: 'VERIFIED_GEO',
          method: 'search_bounded',
          regionKey: null,
          regionName: locationName,
          lat: lat ?? null,
          lng: lng ?? null,
          confidence: 'medium',
          reason: `Lead address "${leadAddress}" contains "${locationName}" (address match)`,
        };
      } else {
        // Address exists but doesn't contain the location — likely out of area
        return {
          status: 'OUT_OF_AREA',
          method: 'search_bounded',
          regionKey: null,
          regionName: locationName,
          lat: lat ?? null,
          lng: lng ?? null,
          confidence: 'medium',
          reason: `Lead address "${leadAddress}" does not contain "${locationName}" — likely outside target area`,
        };
      }
    }
    
    // No address available either — truly unknown
    return {
      status: isHardConstraint ? 'UNKNOWN' : 'SEARCH_BOUNDED',
      method: isHardConstraint ? 'unknown' : 'search_bounded',
      regionKey: null,
      regionName: null,
      lat: lat ?? null,
      lng: lng ?? null,
      confidence: 'low',
      reason: `No region boundary data for "${locationName}" and no address available`,
    };
  }
```

Also add the same address fallback AFTER the bounding box check (at the end of the function, where it currently returns OUT_OF_AREA for bbox misses). Before the final OUT_OF_AREA return, add:

```typescript
  // Bbox says out of area, but double-check with address string
  // (bbox is for the county — the town might be at the edge)
  const locationLower = locationName.toLowerCase().trim();
  const addressLower = (leadAddress || '').toLowerCase();
  if (addressLower && addressLower.includes(locationLower)) {
    return {
      status: 'VERIFIED_GEO',
      method: 'search_bounded',
      regionKey: regionKeys[0],
      regionName: locationName,
      lat,
      lng,
      confidence: 'medium',
      reason: `Lead address contains "${locationName}" despite being outside ${regionNames} bbox (address override)`,
    };
  }
```

═══════════════════════════════════════════════════════════════
CHANGE 2: server/supervisor/cvl.ts — pass address to verifyLocationGeo
═══════════════════════════════════════════════════════════════

In the `verifyOneConstraint` function, find the `case 'LOCATION_EQUALS':` block. The call to `verifyLocationGeo` currently passes 4 arguments:

```typescript
const geoResult: GeoVerificationResult = verifyLocationGeo(
  lead.lat,
  lead.lng,
  locationValue,
  constraint.hard,
);
```

Add the lead's address as the 5th argument:

```typescript
const geoResult: GeoVerificationResult = verifyLocationGeo(
  lead.lat,
  lead.lng,
  locationValue,
  constraint.hard,
  lead.address,
);
```

═══════════════════════════════════════════════════════════════
WHAT THIS FIXES
═══════════════════════════════════════════════════════════════

Before: "find bakeries in Arundel" returns Cyrnel Bakery at "Lower Rd, Forest Row, RH18 5HE"
- resolveRegionKeys("Arundel") → [] (no county match)
- verifyLocationGeo returns UNKNOWN
- Lead stays in delivery as unverified candidate

After: Same query
- resolveRegionKeys("Arundel") → [] (no county match)  
- Fallback: "Lower Rd, Forest Row, RH18 5HE".includes("arundel") → FALSE
- verifyLocationGeo returns OUT_OF_AREA
- Lead rejected from verified delivery (hard constraint failed)

Leads with "Arundel" in their address (e.g. "45 High St, Arundel BN18 9AG") pass correctly:
- "45 high st, arundel bn18 9ag".includes("arundel") → TRUE
- Returns VERIFIED_GEO with medium confidence

═══════════════════════════════════════════════════════════════
DO NOT CHANGE
═══════════════════════════════════════════════════════════════

- The REGIONS bounding box data — keep the county-level data as-is
- The COMPOSITES (Sussex = East Sussex + West Sussex) — working correctly
- LOCATION_NEAR handling — different logic, not affected
- The overall CVL flow — just adding the address parameter pass-through
- Any test files — update them only if the build breaks