You are conducting a forensic investigation of the most recent Wyshbone run to determine whether the UI summary is truthful.

IMPORTANT
This is an investigation only.
Do NOT modify code.
Do NOT change system behaviour.
Do NOT write new artefacts.
Do NOT propose fixes yet.

Your job is to reconstruct the truth from the run data.

Goal
Verify whether the UI summary "7 emails and 9 phone numbers found" is correct.

Scope
Investigate the most recent completed run in the database.

Steps

1. Identify the last completed run_id.

2. Extract all artefacts associated with that run including (but not limited to):

- contact_extract
- lead_pack
- delivery_summary
- tower_judgement
- any other artefacts containing contacts or lead data

3. Determine the set of DELIVERED LEADS.

Use the artefact that represents final delivery (delivery_summary or final lead_pack).

List each delivered lead with:

- name
- place_id (or equivalent id)

4. Reconstruct contacts for those delivered leads.

For each delivered lead:

List:

Emails discovered
Phones discovered

Also record which artefact they came from.

5. Normalize and deduplicate contacts.

Email normalization rules:
- lowercase
- trim whitespace
- strip surrounding punctuation
- remove duplicates

Phone normalization rules:
- remove spaces
- remove punctuation
- standardize format if possible
- remove duplicates

6. Classify email quality.

For each unique email classify as:

CLEAN
MALFORMED

Examples of malformed:

882214admin@redlionarundel.com
anything with numeric garbage prefix
anything failing a basic email regex

7. Produce final counts.

Report:

Total unique emails
Clean emails
Malformed emails
Total unique phones

8. Compare these results to what the UI reported.

UI reported:
7 emails
9 phones

Determine if this is:

ACCURATE
OVERCOUNTED
UNDERCOUNTED

9. Identify the root cause of any mismatch.

Examples may include:

- malformed emails counted as valid
- duplicate contacts counted multiple times
- contacts from non-delivered leads counted
- artefacts double counted

10. Produce a final forensic report.

The report must contain:

RUN ID

Delivered Leads
(list)

Unique Emails
(list)

Email Classification
(clean vs malformed)

Unique Phones
(list)

Final Verified Counts
emails
phones

UI Reported Counts
emails
phones

Verdict
(correct / incorrect)

Root Cause
(clear explanation)

IMPORTANT
Do not speculate.
Only use artefacts from the run.
The goal is to reconstruct the factual truth of the run.