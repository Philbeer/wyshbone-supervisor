The LLM planner was partially implemented — executor metadata was added to the registry, but two critical pieces are missing:

server/supervisor/reloop/llm-planner.ts was never created
server/supervisor/reloop/planner.ts was never updated to become the async wrapper that dispatches to the LLM planner

Please complete both of these now. Refer to the AGENT_LOG.md entry titled "Session: 2026-03-20 — LLM Planner" for the spec that was written. The key requirements:
A. Create server/supervisor/reloop/llm-planner.ts:

Export an async function llmPlan(context) that calls gpt-4o-mini with response_format: { type: 'json_object' }
Build a 4-section system prompt dynamically: role/principles (fixed), executor catalogue (from getAllExecutorMeta()), variable definitions (semi-fixed), session context (dynamic — loop history, mission, constraints, intent narrative)
User message asks which executor to use, expects JSON: { "executor_type": "...", "reasoning": "..." }
10-second timeout via AbortController
Validate executor_type is in availableExecutors, reasoning is non-empty string
On any failure, throw so the caller falls back to rules
Log with [RELOOP_LLM_PLANNER] prefix including latency and token counts
Use the existing OpenAI import pattern: const { default: OpenAI } = await import('openai'); const client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

B. Update server/supervisor/reloop/planner.ts:

Rename the current plan function to rulesPlan (export it)
Create a new async plan function that checks process.env.LLM_PLANNER_ENABLED
If enabled and set to 'true', dynamically import and call llmPlan from ./llm-planner
If disabled or if llmPlan throws, fall back to rulesPlan silently
The function signature must accept the expanded context (mission, constraints, intentNarrative fields) — see the PlannerContext interface in the AGENT_LOG spec

C. Update server/supervisor/reloop/loop-skeleton.ts:

Change plannerPlan(...) call to await plannerPlan(...) since plan is now async
Pass the additional context fields: mission: baseExecutorInput.mission, constraints: { hardConstraints, softConstraints }, and the intentNarrative mapped to the PlannerContext shape

Do NOT modify executor-registry.ts, index.ts, gate.ts, judge-adapter.ts, or any adapter files — those are already correct.
Work on the main branch only — do not create or switch branches. After completing this task, append a structured summary to AGENT_LOG.md (what changed, files modified, decisions made, what's next).