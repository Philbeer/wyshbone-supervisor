You are working in the Wyshbone Supervisor repo.

Goal: Fix 3 production bugs in the core agent run pipeline.

Failing input:
"Find 10 pubs in Arundel and include email"

Observed AFR bug:
Search Query Compiled shows:
query="pubs"
location="arundel and include email"
This is wrong. "include email" is a delivery requirement, not part of location.

Observed delivery bug:
Requested 10 but final_delivery artefact contains 30 leads and requested=10.
This then causes Tower to output nonsense: "No results were found…", even though 30 leads were found and verified.
We must enforce requested_count at final delivery.

Observed state bug:
Delivery Summary shows STOP while AFR shows stop=completed.
We need consistent terminal state bookkeeping.

Tasks:
1) Fix query compilation so delivery requirements like "include email/phone/website" never contaminate location.
   - Parse "and include email" as include_email=true and keep location="Arundel".
   - Ensure compiled query uses ONLY search_query + location.
2) Enforce requested_count at final delivery:
   - Before writing final_delivery, trim the delivered leads array to requested_count.
   - Store found_count_raw separately if useful, but delivered_count must equal requested_count when enough leads exist.
3) Fix run terminal state consistency:
   - If final_delivery is created and final tower judgement is recorded, run state must be COMPLETED.
   - STOP should only be used for early termination (Tower STOP or explicit stop conditions).
   - Ensure Delivery Summary derives from the same canonical state.

Implementation requirements:
- Make the smallest safe code changes.
- Update any relevant types/schemas so final_delivery includes requested_count, delivered_count, and any fields Tower expects (eg constraint_results or equivalent).
- Add or update a unit/integration test for the failing input to assert:
  - location == "Arundel"
  - include_email == true
  - final_delivery.delivered_count == 10
  - run terminal state == COMPLETED

Output requirements:
- Rewrite the full modified files (not partial diffs).
- List the files changed.
- Provide the exact command(s) to run tests and the expected pass output.