Prompt 1 of 5 — Migration + Types
CONTEXT: We're implementing crash recovery for the re-loop architecture (roadmap item 6.2). Currently, every loop iteration writes to the Supabase `loop_state` table, but nothing ever reads it back. When the Supervisor restarts mid-run, orphaned runs restart from scratch. We want to resume from the last completed loop iteration instead.

TASK: Two changes.

1. Create a new migration file `server/migrations/run-loop-state-recovery-migration.ts`. Follow the exact same pattern as the existing `server/migrations/run-reloop-migration.ts` (same imports, same `fatal()` helper, same guard logic, same `Pool` usage with `@neondatabase/serverless` and `ws`). The migration should:

   - `ALTER TABLE loop_state ADD COLUMN IF NOT EXISTS executor_completed BOOLEAN DEFAULT FALSE;`
   - `ALTER TABLE loop_state ADD COLUMN IF NOT EXISTS accumulated_entities JSONB DEFAULT '[]'::jsonb;`
   - `ALTER TABLE loop_state ADD COLUMN IF NOT EXISTS executor_output_full JSONB DEFAULT '{}'::jsonb;`

   The `executor_completed` flag tells recovery whether the executor finished before the crash. The `accumulated_entities` column stores the full entity map (as a JSON array of ExecutorEntity objects) at each loop boundary — this is critical because the in-memory `accumulatedEntityMap` is lost on crash. The `executor_output_full` stores the complete ExecutorOutput for the current loop so we can re-run judge without re-running the executor.

2. Update `server/supervisor/reloop/types.ts`. Add a new `ResumeCheckpoint` interface after the existing `LoopStateRow` interface:
```typescript
export interface ResumeCheckpoint {
  canResume: boolean;
  chainId: string;
  lastCompletedLoop: number;
  resumeFrom: 'planner' | 'judge' | 'full_restart';
  accumulatedEntities: ExecutorEntity[];
  loopHistory: LoopRecord[];
  executorsTriedSoFar: string[];
  lastExecutorOutput?: ExecutorOutput;
  lastPlannerDecision?: PlannerDecision;
  finalRawResult?: Record<string, unknown>;
}
```

   Also update the existing `LoopStateRow` interface to add the three new optional fields:
   - `executor_completed?: boolean;`
   - `accumulated_entities?: string;` (it's stored as JSON string in Supabase)
   - `executor_output_full?: Record<string, unknown>;`

Do NOT modify any other files yet. Do NOT run the migration — I'll run it manually against Supabase.