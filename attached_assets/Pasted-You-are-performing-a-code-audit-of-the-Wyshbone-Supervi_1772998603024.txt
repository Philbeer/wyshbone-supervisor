You are performing a code audit of the Wyshbone Supervisor repository.

Your task is to produce a technical audit report comparing the current implementation against the Wyshbone V1 architecture plan.

Do NOT modify any code.

Only analyse and report.

The V1 architecture defines these core behaviours:

STAGE 1 — INTERPRETATION
User input must convert into a structured mission object with:

entity
location
constraints
mission_mode

Constraint extraction should produce structured constraints with types such as:

entity/category
location
text comparison
relationship
attribute/property
time/date
numeric/range
website evidence
ranking signals

STAGE 2 — DETERMINISTIC PLANNER

The planner must:

create execution strategies based only on the structured mission

Planner improvements expected in V1:

candidate_pool_size = requested_results × 3

relationship direction reasoning
(authority entities such as councils, NHS, universities should be searched first when verifying relationships)

source preference heuristics
(prefer councils, universities, NHS bodies, large organisations, grant makers)

STAGE 3 — EVIDENCE LAYER

Evidence extraction should produce structured evidence items including:

source URL
direct quote
context snippet

Evidence should be constraint-led rather than general crawler summaries.

Tools should support fallback behaviour when crawling fails.

STAGE 4 — REPLAN LOOP

The system should support:

PLAN
EXECUTE
TOWER JUDGE
REPLAN
EXECUTE
STOP

Supervisor should generate new strategies when Tower fails due to:

weak evidence
crawler blocked
relationship verification failure
insufficient candidate pool

max_replans should be limited (1–2)

Your job:

Audit the codebase and produce a report with the following sections:

1. Interpretation Layer
What code performs constraint extraction and mission creation?
What schema defines the mission object?

2. Planner Logic
Where the planner lives.
How strategies are created.
Whether candidate pool expansion exists.

3. Relationship Discovery Logic
Does the planner reason about discovery direction?
If so, how?

4. Evidence Handling
What evidence structure is passed to Tower?
Is quote extraction implemented?

5. Replan Loop
Where replanning logic exists.
How failure reasons are interpreted.
How new plans are generated.

6. Tool Fallback Logic
If a tool fails, how does Supervisor react?

7. Missing Pieces
List gaps relative to the V1 plan.

Output a structured technical report.
Do not change code.