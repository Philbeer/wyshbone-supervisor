Goal: Activate Agent Loop v1 for real SEARCH_PLACES runs, so “Completed” only happens after Tower judgement + Supervisor reaction + run_summary.

Current failure: runs mark Completed after one tool call even when Delivered << Requested. Claude Chrome showed 200 requested, 20 delivered, no replan.

Implement:
A) Ensure every SEARCH_PLACES execution produces a leads_list artefact that includes success_criteria.target_count = requested_count (eg 200) and delivered_count.
- If requested count exists in the user message or mission params, pass it through.
- Default target_count to 20 if not specified.

B) Immediately after posting leads_list artefact, call Tower verdict v1 endpoint with:
   { goal, success_criteria, artefact: leads_list }
Expect response verdict ∈ {ACCEPT, RETRY, CHANGE_PLAN, STOP} plus delivered/requested, gaps, confidence, rationale.

C) React to verdict (real loop):
- ACCEPT: emit run_completed and post run_summary artefact (Delivered vs Requested, gaps, confidence, Tower verdict).
- RETRY: rerun SEARCH_PLACES once (maxRetries=1). After rerun, judge again. If still RETRY or delivered < 50% of target, STOP with summary.
- CHANGE_PLAN: create plan_update artefact (Plan v2) that explicitly expands search (increase radius or broaden location) and rerun once (maxPlanVersions=2). After rerun, judge again. If still CHANGE_PLAN, STOP with summary.
- STOP: emit run_stopped and post run_summary with stop_reason from Tower.

D) The plan change for CHANGE_PLAN should be concrete and deterministic for now:
- If location is a town (eg Arundel), expand to “within 10km”, then “within 25km” (only one change allowed in v1).
- Or broaden query: “pub OR bar” if too few.
Pick ONE approach and document it in plan_update payload.

E) Emit AFR events in order:
mission_received → router_decision → tool_call_started → tool_call_completed → artefact_post_succeeded → artefact_created → tower_judgement → (run_completed OR run_stopped)
(Do not emit run_completed before tower_judgement.)

F) Add one structured log line at the end:
[AGENT_LOOP] runId=... target=200 delivered=20 verdict=CHANGE_PLAN planVersion=2 retries=0 stopped=false

Acceptance test:
- Ask: “Find 200 pubs in Arundel using Google Places.”
- First pass likely returns ~20.
- Tower returns CHANGE_PLAN.
- Supervisor reruns with expanded radius.
- UI shows plan_update + run_summary with Delivered vs Requested and Tower verdict badge.
- If still far below 200, STOP with honest summary.

Do the minimal changes necessary. Keep other tools unchanged.
