We have a persistent production issue: leadgen shortfall runs (e.g. “find 4 pubs in Arundel with ‘swan’ in the name”) halt at Plan v1 with Tower verdict fail and action stop. We expect Tower to return change_plan with an EXPAND_AREA suggestion so Supervisor enters the replan loop and expands radius.

We already added a guarded debug log (DEBUG_TOWER_PAYLOAD=true) in server/supervisor/tower-artefact-judge.ts right before the fetch to Tower. Despite this, live runs still show stop.

Your job: produce an evidence-based investigation and a plan.

Deliverables:

Confirm which Tower instance we are calling at runtime:

print resolved TOWER_BASE_URL

print full endpoint path used (e.g. /api/tower/judge-artefact)

confirm env values loaded (dev/prod)

Capture the exact outbound request body for the Swan run:

runId / artefactId / artefactType

successCriteria.target_count / requested_count

constraints (typed) length + first 2 items (redacted if needed)

hard_constraints / soft_constraints length + first 2

prefix / location / plan_version
Output this as a single [DEBUG_TOWER_PAYLOAD] block so we can paste it into chat.

Determine whether the constraints are missing at the source:

Are we generating typed constraints anywhere?

Are we only sending legacy hard_constraints/soft_constraints?

Are we sending neither?
Identify where constraints should be derived from the user message and where they are being dropped.

Determine whether the replan loop is reachable:

Confirm the gate while (finalAction === 'change_plan') is still the only entry.

Confirm max_replans / replan budget values being sent to Tower and used locally.

Conclusion (single root cause):

Choose ONE: (a) wrong Tower instance / stale deploy, (b) outbound payload lacks constraints, (c) payload has constraints but Tower still returns stop, (d) something else (prove it).

Plan:

Step-by-step plan with the smallest safe change(s) to restore expected behaviour:

If payload missing constraints → implement typed constraints in Supervisor OR add missing legacy fields.

If wrong Tower instance → fix env/config/deploy wiring.

If Tower returning stop despite constraints → coordinate with Tower fix.

Include how we will validate the fix (expected log sequence: Tower action change_plan → Plan v2 → radius expansion).

Rules:

No code changes beyond logging unless necessary to gather evidence.

Give exact file + line references for where you found each behaviour.

Keep it production-grade (no hacks, no manual steps required each run).