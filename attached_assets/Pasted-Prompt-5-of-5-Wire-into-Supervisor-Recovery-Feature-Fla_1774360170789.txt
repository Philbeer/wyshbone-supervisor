Prompt 5 of 5 — Wire into Supervisor Recovery + Feature Flag
CONTEXT: Final step of 6.2 crash recovery. The loop-skeleton can now accept a checkpoint and resume. Now we wire it into the Supervisor's task recovery pipeline and the execution entry point.

TASK: Three files to modify.

FILE 1: `server/supervisor/mission-executor.ts`

In the `MissionExecutionContext` interface, add an optional field:
```typescript
checkpoint?: import('./reloop/types').ResumeCheckpoint | null;
```

In the `executeMissionWithReloop` function, where it calls `runReloop(...)`, pass through the checkpoint:
```typescript
result = await runReloop({
  runId: ctx.runId,
  userId: ctx.userId,
  conversationId: ctx.conversationId,
  clientRequestId: ctx.clientRequestId,
  rawUserInput: ctx.rawUserInput,
  mission: ctx.mission,
  plan: ctx.plan,
  missionTrace: ctx.missionTrace,
  intentNarrative: ctx.intentNarrative,
  queryId: ctx.queryId,
  executionPath: ctx.executionPath,
  checkpoint: ctx.checkpoint ?? null,   // <-- ADD THIS LINE
});
```

FILE 2: `server/supervisor.ts`

2a. In the `evaluateAndRecoverTask` method, in the section where it requeues a task (the block that calls `supabase.from('supervisor_tasks').update({ status: 'pending' })`), also set a recovery flag on the agent_run metadata. Find the existing `storage.updateAgentRun(runId, { status: 'executing', metadata: { ...existingMetadata, recovery_attempts: attempts, ... } })` call and add `resume_from_loop_state: true` to the metadata object.

2b. In `processChatTask`, right BEFORE the block that builds `missionCtx` and calls `executeMissionWithReloop` (search for `const missionCtx: MissionExecutionContext`), add checkpoint detection:
```typescript
// ── 6.2 Crash Recovery: check for resumable loop state ──
let recoveryCheckpoint: import('./supervisor/reloop/types').ResumeCheckpoint | null = null;
const crashRecoveryEnabled = (process.env.CRASH_RECOVERY_ENABLED || 'false').toLowerCase() === 'true';

if (crashRecoveryEnabled) {
  try {
    const existingRun = await storage.getAgentRunById?.(jobId) ?? null;
    const shouldCheckResume = existingRun?.metadata?.resume_from_loop_state === true;
    
    if (shouldCheckResume) {
      const { checkForResumableState } = await import('./supervisor/reloop/resume');
      recoveryCheckpoint = await checkForResumableState(jobId);
      
      if (recoveryCheckpoint) {
        console.log(`[CRASH_RECOVERY] Run ${jobId}: resuming from loop ${recoveryCheckpoint.lastCompletedLoop + 1} phase=${recoveryCheckpoint.resumeFrom} entities=${recoveryCheckpoint.accumulatedEntities.length}`);
        
        await createArtefact({
          runId: jobId,
          type: 'diagnostic',
          title: `Crash recovery activated for run ${jobId}`,
          summary: `Resuming from loop ${recoveryCheckpoint.lastCompletedLoop + 1} (${recoveryCheckpoint.resumeFrom}). ${recoveryCheckpoint.accumulatedEntities.length} entities recovered.`,
          payload: {
            resume_from: recoveryCheckpoint.resumeFrom,
            last_completed_loop: recoveryCheckpoint.lastCompletedLoop,
            accumulated_entities: recoveryCheckpoint.accumulatedEntities.length,
            executors_tried: recoveryCheckpoint.executorsTriedSoFar,
          },
          userId: task.user_id,
          conversationId: task.conversation_id,
        }).catch(() => {});
      } else {
        console.log(`[CRASH_RECOVERY] Run ${jobId}: no resumable state found, will restart from scratch`);
      }
    }
  } catch (recoveryErr: any) {
    console.warn(`[CRASH_RECOVERY] Run ${jobId}: recovery check failed (non-fatal, proceeding with full restart): ${recoveryErr.message}`);
    recoveryCheckpoint = null;
  }
}
```

Then pass it through to the MissionExecutionContext. Find the line:
```typescript
const missionCtx: MissionExecutionContext = {
```
and add `checkpoint: recoveryCheckpoint,` as the last field in that object literal.

NOTE: `storage.getAgentRunById` may not exist. Check the `storage` object in `server/storage.ts`. If there's a method like `getAgentRun` or `getAgentRunById` that takes a run ID and returns the run with metadata, use that. If not, query Supabase directly:
```typescript
const { data: existingRunData } = await supabase!
  .from('agent_runs')
  .select('id, status, metadata')
  .eq('id', jobId)
  .maybeSingle();
const shouldCheckResume = existingRunData?.metadata?.resume_from_loop_state === true;
```

FILE 3: `server/supervisor/reloop/index.ts`

The `runReloop` is re-exported from this file. No changes needed to the re-export since we only added an optional parameter. But verify the export line `export { runReloop } from './loop-skeleton';` is still correct.

Also add an export for the resume function:
```typescript
export { checkForResumableState } from './resume';
```

ALSO: In `server/supervisor.ts`, in the `recoverOrphanedAgentRuns` method and in `sweepOrphanedAgentRuns`, after the existing code that marks a stuck agent_run as failed, add cleanup of its loop_state rows:
```typescript
// Clean up stale loop_state rows for this orphaned run
if (supabase) {
  await supabase
    .from('loop_state')
    .update({ status: 'circuit_broken', completed_at: new Date().toISOString() })
    .eq('run_id', run.id)
    .eq('status', 'active')
    .then(() => console.log(`[RECOVERY_RUNS] Closed active loop_state rows for run ${run.id}`))
    .catch((e: any) => console.warn(`[RECOVERY_RUNS] Failed to close loop_state rows for run ${run.id}: ${e.message}`));
}
```

Add this right after the existing `storage.updateAgentRun(run.id, { status: 'failed', ... })` call in BOTH methods.

Feature flag: `CRASH_RECOVERY_ENABLED` defaults to `false`. Add it to Replit secrets set to `false` for now. We'll flip it to `true` after testing.

After all changes, do a TypeScript compile check (`npx tsc --noEmit`) to verify no type errors.