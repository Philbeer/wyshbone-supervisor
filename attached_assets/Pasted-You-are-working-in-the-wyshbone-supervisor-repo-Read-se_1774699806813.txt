You are working in the wyshbone-supervisor repo. Read server/supervisor/google-places.ts before making changes.

TASK: Add search-level caching to the searchPlaces function. The file already has a placeCache (per place_id) and geocodeCache — but the actual TEXT SEARCH results are NOT cached. This means every identical search ("pubs in Sussex") hits Google Places API fresh every time, at £0.025 per call.

Add a searchCache Map that caches the FULL search result by query+location+country+maxResults+mode key, with a 24-hour TTL. Also add periodic stats logging.

SPECIFIC CHANGES:

1. After the existing geocodeCache declaration (~line 71), add:

// ---------------------------------------------------------------------------
// In-memory search results cache — keyed by query+location+country+mode
// Prevents identical searches from hitting Google repeatedly (24h TTL)
// ---------------------------------------------------------------------------
interface SearchCacheEntry {
  data: SearchPlacesResult;
  expiresAt: number;
  createdAt: number;
}
const searchResultsCache = new Map<string, SearchCacheEntry>();
let cacheStats = { hits: 0, misses: 0 };

// Periodic cleanup and stats logging
setInterval(() => {
  const now = Date.now();
  let evicted = 0;
  for (const [key, entry] of searchResultsCache) {
    if (now > entry.expiresAt) { searchResultsCache.delete(key); evicted++; }
  }
  const total = cacheStats.hits + cacheStats.misses;
  const hitRate = total > 0 ? Math.round((cacheStats.hits / total) * 100) : 0;
  console.log(`📊 [GP CACHE] Hits: ${cacheStats.hits} | Misses: ${cacheStats.misses} | Rate: ${hitRate}% | Entries: ${searchResultsCache.size}${evicted ? ` | Evicted: ${evicted}` : ''}`);
}, 10 * 60 * 1000); // Every 10 minutes

2. At the TOP of the searchPlaces function, BEFORE the apiKey check, add this cache lookup:

  // ─── SEARCH CACHE CHECK ───────────────────────────────────────
  const cacheKey = `search:${query.toLowerCase().trim()}||${location.toLowerCase().trim()}||${country.toLowerCase().trim()}||${maxResults}||${mode}`;
  const cachedSearch = searchResultsCache.get(cacheKey);
  if (cachedSearch && Date.now() < cachedSearch.expiresAt) {
    cacheStats.hits++;
    const ageMin = Math.round((Date.now() - cachedSearch.createdAt) / 60000);
    console.log(`⚡ [GP CACHE HIT] "${query}" in "${location}" (age: ${ageMin}m) — saved 1+ API call`);
    return cachedSearch.data;
  }
  cacheStats.misses++;

3. Just BEFORE the final "return { success: true, places, debug: ... }" at the end of the try block, add:

    // ─── CACHE THE RESULT ─────────────────────────────────────────
    const resultToCache: SearchPlacesResult = {
      success: true,
      places,
      debug: buildDebug(requestedMode, usedMode, searchQuery, regionUsed, biasApplied, biasLocation, radiusUsed, pagesFetched, places.length, biasFallback),
    };
    searchResultsCache.set(cacheKey, {
      data: resultToCache,
      expiresAt: Date.now() + 24 * 60 * 60 * 1000, // 24 hour TTL
      createdAt: Date.now(),
    });
    console.log(`💾 [GP CACHE] Stored: "${query}" in "${location}" (${places.length} results, cached for 24h)`);

Then return resultToCache instead of building a new object.

4. Do NOT change any function signatures, exports, field masks, or other logic. Only add the cache lookup at the top and cache store before the return.

VERIFICATION: After applying, grep for "GP CACHE" and confirm you see the cache check, cache store, and stats logger.