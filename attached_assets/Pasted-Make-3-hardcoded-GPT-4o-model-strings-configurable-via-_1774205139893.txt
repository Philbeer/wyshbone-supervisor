Make 3 hardcoded GPT-4o model strings configurable via environment variables. No other changes.
Change 1 — GPT-4o Fallback (mission-executor.ts)
In server/supervisor/mission-executor.ts, find the GPT-4o web search fallback section (around the comment // ── GPT-4o web search fallback). Inside the fetch('https://api.openai.com/v1/responses', ...) call body, change:
model: 'gpt-4o',
to:
model: process.env.GPT4O_FALLBACK_MODEL ?? 'gpt-4o-mini',
There is only ONE occurrence of this fetch call in the fallback section. Log the model used:
const fallbackModel = process.env.GPT4O_FALLBACK_MODEL ?? 'gpt-4o-mini';
console.log(`[GPT4O_FALLBACK] Using model: ${fallbackModel}`);
Add this log line just before the fetch call.
Change 2 — Discovery Cascade (mission-executor.ts)
In the same file, find the discovery cascade section (around the comment // ── Discovery cascade). Inside the fetch('https://api.openai.com/v1/responses', ...) call body, change:
model: 'gpt-4o',
to:
model: process.env.GPT4O_CASCADE_MODEL ?? 'gpt-4o-mini',
Again log the model used just before the fetch call:
const cascadeModel = process.env.GPT4O_CASCADE_MODEL ?? 'gpt-4o-mini';
console.log(`[DISCOVERY_CASCADE] Using model: ${cascadeModel}`);
Change 3 — GPT-4o Primary Search (gpt4o-search.ts)
In server/supervisor/gpt4o-search.ts, find the responses.create call inside callGpt4oWebSearch. Change:
model: 'gpt-4o',
to:
model: process.env.GPT4O_PRIMARY_MODEL ?? 'gpt-4o',
Note: this one defaults to gpt-4o (not mini). Log it too:
const primaryModel = process.env.GPT4O_PRIMARY_MODEL ?? 'gpt-4o';
console.log(`[GPT4O_SEARCH] Using model: ${primaryModel}`);
Then create these Secrets (if they don't already exist):

GPT4O_FALLBACK_MODEL = gpt-4o-mini
GPT4O_CASCADE_MODEL = gpt-4o-mini
GPT4O_PRIMARY_MODEL = gpt-4o

Summary: 3 model strings changed from hardcoded to env-configurable. Fallback and cascade default to mini. Primary defaults to gpt-4o. Each logs which model it's using at runtime. No other code changes.

Paste that straight in. Once it's done, you'll see [GPT4O_FALLBACK] Using model: gpt-4o-mini in your logs to confirm it's working. And if you ever want to switch any of them back, just change the Secret value and restart — no code change needed.