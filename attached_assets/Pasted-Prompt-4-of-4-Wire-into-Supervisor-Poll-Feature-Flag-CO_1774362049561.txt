Prompt 4 of 4 — Wire into Supervisor Poll + Feature Flag
CONTEXT: Final step of 6.1 Sleep/Wake Goals. The wake scheduler is built. Now we wire it into the Supervisor's poll loop so it runs automatically.

TASK: Modify `server/supervisor.ts`.

1. In the `poll()` method, find the array of tasks that runs on each poll tick:
```typescript
   const tasks: Promise<void>[] = [
     this.processNewSignals(),
     this.processSupervisorTasks(),
     this.monitorGoals(),
     this.flagBypassedRuns(),
   ];
```
   Add a new task:
```typescript
   const tasks: Promise<void>[] = [
     this.processNewSignals(),
     this.processSupervisorTasks(),
     this.monitorGoals(),
     this.flagBypassedRuns(),
     this.wakeGoals(),  // 6.1: Sleep/wake goal scheduler
   ];
```

2. Add the `wakeGoals` method to the `SupervisorService` class:
```typescript
   private async wakeGoals(): Promise<void> {
     try {
       const { checkAndWakeGoals } = await import('./supervisor/sleep-wake/index');
       await checkAndWakeGoals();
     } catch (error: any) {
       // Never let wake failures crash the poll loop
       console.error(`[SLEEP_WAKE] Wake check failed (non-fatal): ${error.message}`);
     }
   }
```

   Using dynamic import so the feature can be completely dead code when `SLEEP_WAKE_ENABLED` is not set.

3. Also in `processChatTask`, find the section that creates a `scheduled_monitors` row (search for `supabase!.from('scheduled_monitors').insert`). Verify that `conversation_id: task.conversation_id` is included in the insert payload. If prompt 1 already added it, just verify. If not, add it now.

4. Also add `conversation_id` to the config object stored in the monitor:
   Find the `config: { ... }` object in the scheduled_monitors insert. Add `conversation_id: task.conversation_id` inside the config object too (as a backup — the top-level column is the canonical source, but having it in config means older code can still find it).

Feature flags:
- `SLEEP_WAKE_ENABLED=false` — add to Replit secrets. The wake scheduler silently no-ops when this is false.
- When ready to test, flip to `true`.

After all changes, run `npx tsc --noEmit` to verify no type errors.

IMPORTANT: Check that the StructuredMission type in `server/supervisor/mission-schema.ts` is compatible with what wake-executor.ts builds. If StructuredMission has required fields that we're not setting in the wake executor, either add sensible defaults or make them optional. The key fields are: `entity_category`, `location_text`, `constraints`, `requested_count`. Look at the actual type definition and make sure the wake executor's mission object satisfies it.