Execution mode: Sequential

Goal
Validate whether the unchecked-lead bug is actually fixed in real execution using the provided AFR run.

Context

A regression test for the hard evidence filter passes, and the code change:

if (!leadsChecked.has(i)) return false;

exists in mission-executor.ts.

However, the attached AFR run shows a case where:

• a query requiring website evidence delivered 5 leads
• only 3 leads carried verification data

This suggests unverified or unchecked leads still passed through somewhere in the pipeline.

Your task is to trace the real runtime path and explain how this could still happen.

AFR evidence (symptoms)

Query type:
Hard website evidence constraint
Example query:
"Find pubs in Arundel that mention live music on their website"

Observed outcome:

Delivered leads: 5
Leads with verification data: 3

This implies that 2 leads survived delivery without passing evidence checks.

Investigation tasks

1. Trace the full pipeline:

candidate leads
→ enrichment
→ evidence collection
→ semantic verification
→ leadsChecked population
→ hard evidence filtering
→ final delivery list

2. Confirm where applyHardEvidenceFilter() is called.

3. Verify whether every delivery path goes through this filter.

4. Identify any conditions where leads may bypass the filter such as:

• evidenceResults.length === 0
• fallback delivery logic
• leads skipped due to batch limits
• leads skipped because website field is null
• mission fallback paths
• legacy constraint pipeline
• ranking-only delivery paths

5. Specifically check whether this condition still exists anywhere:

if (hardEvidenceConstraints.length > 0 && evidenceResults.length > 0)

If evidenceResults.length === 0 disables the filter,
then the fix will not actually run.

6. Determine whether any code path rebuilds the lead list after filtering.

7. Verify if leads without websites can still pass a website_evidence constraint.

Deliverables

1. A clear explanation of whether the bug is truly fixed.
2. If not fixed, identify the exact bypass condition.
3. Show the exact file + lines responsible.
4. Provide the minimal safe patch to close the bypass.
5. Write findings to:

SUPERVISOR_EVIDENCE_FILTER_RUNTIME_AUDIT.md