You are working in the Wyshbone Supervisor repo. Goal: prevent the critical trust failure where the agent executes under the wrong semantic frame, and prevent delivery when the run is FAIL/STOP/ERROR.

Implement production-grade guardrails in Supervisor. Tight diff; no big refactors.

A) ENFORCE “RUNNABLE VS CLARIFY” GATE EVEN IN AGENT-FIRST
Currently agent-first flows can execute immediately.
Add a pre-plan gate that runs before creating Plan v1:
- If the user_message is informational (question-like) OR mismatched with the active vertical OR too ambiguous to run safely, return a CLARIFY response instead of starting tools.
- CLARIFY response must be a structured artefact + normal chat message that UI can render (eg artefact type: clarification_needed) with:
  - reason
  - suggested clarification question
  - optional “assumptions I could make”
Do NOT call SEARCH_PLACES when clarification_needed=true.

B) VERTICAL MISMATCH GUARD (Supervisor-side, must exist even if UI fails)
Add a simple vertical mismatch heuristic:
- If vertical != “general” and the user_message contains strong mismatch keywords for that vertical, set clarification_needed=true and ask user whether to switch to general.
Start with Breweries mismatch keywords matching the QA failure (“vulnerable adults”, “support”, “social care”, “benefits”, “housing”, “charity”, “council services”, etc.).
Keep it config-driven and minimal.

C) CLEAN QUERY NORMALIZATION (prevent merged queries)
Before passing to SEARCH_PLACES:
- Normalize whitespace.
- If the message contains two intents jammed together (basic heuristic: a location phrase for one query followed immediately by “what/which/how/where” etc with no separator, or contains two different location entities), then clarification_needed=true.
Add a guard that logs a “query_suspected_merged=true” flag into decision_log artefact.

D) HARD TRUTH CONTRACT: DO NOT DELIVER ON FAIL/STOP/ERROR
Right now you can produce leads_list and delivery_summary even when Tower judgement fails or run verdict is fail.
Implement:
- delivery_summary.status MUST be derived from final verdict, not from leads count.
Rules:
  - If final verdict is FAIL/STOP -> delivery_summary.status=FAIL and delivered_count=0 (or delivered_count but explicitly marked UNTRUSTED, pick one and be consistent)
  - If Tower judgement cannot be reached (HTTP 503 etc), mark tower_status=ERROR and delivery_summary.status=ERROR (not PASS).
  - The system must never emit “PASS” when tower_status=ERROR or verdict=FAIL.
Also ensure the final chat message to UI includes the final verdict explicitly (PASS/FAIL/STOP/ERROR) so UI can enforce.

E) VERIFIED LABELS MUST REQUIRE VERIFICATION
If verification is not_applicable, do not mark leads “verified”.
Only set verified_exact when CVL verified it (or when verification layer explicitly confirms).
Otherwise mark candidate/unverified.

Acceptance tests:
1) Reproduce QA case: vertical=Breweries, user asks “what organisations support vulnerable adults in leeds”.
Expected: Supervisor returns clarification_needed artefact asking to switch to general. No SEARCH_PLACES call.
2) Simulate Tower 503: run still may discover leads but final delivery_summary must be ERROR (not PASS) and final verdict must be ERROR; UI should not show verified results.
3) Normal venue query with no hard constraints: still runs and returns PASS.

Deliver:
- list touched files
- show the final truth-contract fields you added to artefacts/messages
- keep it minimal