Code cleanup: Remove ALL legacy clarification code and add guards against LLM preamble leaking.

PART 1 — REMOVE DEAD PASS3_CLARIFY CODE

In server/supervisor.ts, find ALL blocks that reference PASS3_CLARIFY. There should be 
two disabled blocks (around lines 1246 and 1254) that look like:

  if (missionMode === 'active' && missionResult?.intentNarrative?.clarification_needed ...) {
    console.log(`[PASS3_CLARIFY] DISABLED — ...`);
  }

DELETE both of these entire if blocks. They are dead code.

PART 2 — REMOVE clarification_needed GATE CHECKS

The clarification_needed flag from Pass 3 was the source of the original bug — the LLM 
non-deterministically sets it to true even when the query is clear. All current 
clarification is handled by the deterministic preflight probe. 

Find and remove or simplify ALL code that checks:
  missionResult?.intentNarrative?.clarification_needed

There are likely 3-4 occurrences. For each one, show me what you find and either:
a) Delete the block if it's dead code (no side effects)
b) Simplify the condition to remove the clarification_needed check if the surrounding 
   code does other useful work

Do NOT remove the intentNarrative object itself — it's used for entity_description, 
findability, key_discriminator, exclusions, and suggested_approaches which are all 
still needed.

PART 3 — GUARD AGAINST LLM PREAMBLE IN MESSAGES

In the sanitizeSupervisorMessage function (near the top of supervisor.ts), find the 
existing count-claim guard:

  if (SUPERVISOR_COUNT_CLAIM_RE.test(msg)) {
    console.warn(`[SUPERVISOR_MSG_GUARD] Blocked count-claiming message: ...`);
    return SUPERVISOR_NEUTRAL_MESSAGE;
  }

Add this block IMMEDIATELY AFTER it:

  // Block conversational preamble that LLMs generate as "helpful" ack messages
  const PREAMBLE_RE = /I'd be happy to|I would be happy to|Could you tell me what|what type of businesses|what kind of businesses|Let me help you find|I can help you|I'll help you find/i;
  if (PREAMBLE_RE.test(msg)) {
    console.warn(`[SUPERVISOR_MSG_GUARD] Blocked conversational preamble: "${msg.substring(0, 120)}…"`);
    return SUPERVISOR_NEUTRAL_MESSAGE;
  }

PART 4 — REPORT

After making changes, report:
- How many PASS3_CLARIFY blocks were removed
- How many clarification_needed checks were removed/simplified  
- Total lines of dead code removed
- Confirm the preamble guard is in sanitizeSupervisorMessage

Do NOT remove evaluatePreflightClarify — that is the CURRENT working system.
Do NOT remove the constraint gate — that is working correctly.
Do NOT modify mission-extractor.ts in this change.